- **[03-01 Task 2] Pre-existing `astro` moderate advisory GHSA-4g3v-8h47-v7g6** — Reflected XSS via unescaped View Transition animation properties, affects `astro` 2.9.0–7.0.9. Surfaced by `npm audit` during the Plan 03-01 dependency install; NOT introduced by `@astrojs/rss` or `sanitize-html`. Not exploitable here: the site does not use View Transitions and ships zero client JS. Out of scope for this plan (pre-existing, unrelated file). Resolve by bumping `astro` past 7.0.9 when a fixed release exists.
