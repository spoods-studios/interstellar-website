---
phase: 02-content-rendering-templating
plan: 08
type: execute
wave: 5
depends_on: ["02-05", "02-06", "02-07"]
files_modified:
  - tests/site.smoke.sh
  - tests/collections.smoke.sh
  - tests/markdown.smoke.sh
  - src/pages/collection-counts.json.ts
  - src/pages/markdown-render-check.astro
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/02-content-rendering-templating/02-UI-SPEC.md
  - .planning/phases/02-content-rendering-templating/02-VALIDATION.md
autonomous: false
requirements: [SITE-04, CONT-02, CONT-03, CONT-04]
must_haves:
  truths:
    - "Every built page emits a canonical URL and sitemap.xml lists every route"
    - "No internal link in the built site points at a page that does not exist"
    - "A drafted entry appears in no index and no generated cross-link list"
    - "A malformed filename and an unresolvable link each fail the build naming the offending file"
    - "The site is legible on mobile and desktop, the custom 404 serves live under the Pages base path, and rendered post text matches its source"
    - "REQUIREMENTS.md and ROADMAP.md describe this phase's real, expanded scope"
  artifacts:
    - tests/site.smoke.sh
  key_links:
    - "The dead-link sweep is the only check that catches cross-plan link breakage, since each wave-4 plan generated links into routes another plan owned"
    - "The live 404 check is the empirical resolution of 02-RESEARCH.md Pitfall 6, which docs and search left at MEDIUM confidence"
---

<orchestrator_note added="2026-07-22" source="wave-2 post-merge gate">
**Extra required task — remove temporary diagnostic routes before the phase closes.**

Plan 02-03 created two build-verification routes that appear in NO plan and were
never intended to ship publicly:

- `src/pages/collection-counts.json.ts` → builds to `/collection-counts.json`
- `src/pages/markdown-render-check.astro` → builds to `/markdown-render-check/`

Both are honestly documented in their own source comments as stand-ins that exist
only because, at wave 2, no real route yet queried the collections or rendered a
Markdown body — 02-03 needed *something* to assert against. Waves 3–4 (plans
02-04 through 02-07) create the real index and detail routes, which makes both
redundant. Left in place they would publish an internal count endpoint and a bare
unstyled render-test page on a public marketing site.

Required:
1. Delete both files.
2. Repoint the two smoke tests that currently depend on them onto real routes:
   - `tests/collections.smoke.sh:10` reads `dist/collection-counts.json` — reassert
     the same four collection counts against the real index pages instead.
   - `tests/markdown.smoke.sh:17` reads `dist/markdown-render-check/index.html` —
     reassert the wikilink-resolution and Shiki-light-theme checks against the real
     rendered deep-dive page for the same entry
     (`m0.3/phase-14.5-swapchain-acquire-fix`).
   Do not weaken either assertion — the coverage they provide (RESEARCH Pitfall 1's
   silent-empty-collection guard, and end-to-end proof the Sätteri plugin runs in
   the real pipeline rather than only via direct `markdownToHtml()` calls) must
   survive the move.
3. Assert in acceptance criteria that neither path exists in `dist/` after a build.
</orchestrator_note>

<objective>
Close the phase: finish SITE-04's verifiable half, prove the loud-fail and draft-filtering paths that only manual fixtures can exercise, run the manual verifications 02-VALIDATION.md reserves for a human, and amend the requirement wording that predates this phase's expansion.

Purpose: Plans 01–07 each verified their own slice. This plan verifies the whole, and closes the documentation debt 02-RESEARCH.md flagged rather than leaving the mismatch undocumented.
Output: `tests/site.smoke.sh`, amended `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`, and updated phase artifacts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-content-rendering-templating/02-CONTEXT.md
@.planning/phases/02-content-rendering-templating/02-RESEARCH.md
@.planning/phases/02-content-rendering-templating/02-VALIDATION.md
@.planning/phases/02-content-rendering-templating/02-UI-SPEC.md
</context>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Notes |
|---|---|---|
| `tests/site.smoke.sh` | harness | Whole-site assertions: canonical coverage, sitemap completeness, dead-link sweep, zero client JS |
| Amended `.planning/REQUIREMENTS.md` | doc | CONT-02 / CONT-04 / CONT-07 text updated to the D-31…D-44 scope |
| Amended `.planning/ROADMAP.md` | doc | Phase 2 goal and success criteria updated to name all four content trees |
| Amended `02-VALIDATION.md` | doc | Verification map bound to real plans; Manual-Only table reduced to the three genuinely manual checks once Task 2 automates D-33/D-39 |

<tasks>

<task type="auto">
  <name>Task 1: Whole-site SITE-04 sweep — canonical, sitemap, dead links, zero JS</name>
  <files>tests/site.smoke.sh</files>
  <read_first>
    - `src/layouts/BaseLayout.astro` — where the canonical link and favicon reference are emitted
    - `astro.config.mjs` — the sitemap integration and the base path every emitted URL must carry
    - `tests/run-all.sh` — the runner that will pick this file up automatically
    - `.planning/phases/02-content-rendering-templating/02-VALIDATION.md` — the SITE-04 row and the Sampling Rate section defining the phase gate
    - `.planning/phases/02-content-rendering-templating/02-RESEARCH.md` — the tertiary-source note that sitemap base-path prefixing was never independently confirmed and should be spot-checked rather than assumed
  </read_first>
  <action>
Create `tests/site.smoke.sh` running a clean build and then asserting across the whole of `dist/`:
  - Every emitted HTML page carries exactly one canonical link element, and every canonical href begins with the configured site plus base. Derive the expected prefix by reading `astro.config.mjs`, not by hardcoding it in the script.
  - `sitemap.xml` (or the sitemap index plus its referenced files) exists, its URLs carry the base path, and the number of listed URLs matches the number of built HTML pages, allowing for the 404 page which is legitimately absent from a sitemap. Report the two counts on mismatch so a failure is diagnosable.
  - Every page references the favicon.
  - Zero `<script` tags anywhere in `dist/` — the whole site is build-time only in this phase.
  - Dead-link sweep: extract every internal href from every built page, normalise it against the base path, and assert a corresponding file exists in `dist/`. This is the only check that catches cross-plan breakage, because Plans 05, 06 and 07 each generated links into routes another plan owned. Ignore fragment-only hrefs and external URLs; report every dead target rather than exiting on the first.
  - Expected total page count sanity check: the four trees plus indexes plus the standalone and error pages.

Make the script executable and confirm `tests/run-all.sh` discovers it.
  </action>
  <verify>
    <automated>npm run build && bash tests/site.smoke.sh && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/site.smoke.sh` exits 0
    - The dead-link sweep reports zero dead internal targets across the whole built site
    - Every built HTML page has exactly one canonical link element carrying the configured base path
    - The sitemap URL count matches the built page count (404 excluded) and every sitemap URL carries the base path
    - `grep -rl '<script' dist/ | wc -l` outputs `0`
    - `npm test` runs every committed harness and exits 0
  </acceptance_criteria>
  <done>The whole site is verified for canonical coverage, sitemap completeness, link integrity and zero client JS in one committed check.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the loud-fail and draft-filter paths with trap-and-restore fixtures</name>
  <files>tests/site.smoke.sh</files>
  <read_first>
    - `tests/build.smoke.sh` — the existing D-10 negative block's trap-and-restore discipline, which these fixtures must follow so the read-only content trees are never left dirty
    - `tests/collections.smoke.sh` — the malformed-filename negative case already covering D-33, so this task does not duplicate it
    - `src/lib/content-guards.ts` — the `isVisible` guard whose coverage is being proven
    - `.planning/phases/02-content-rendering-templating/02-VALIDATION.md` — the D-30, D-33 and D-39 rows and the Manual-Only Verifications table
  </read_first>
  <action>
Add three negative cases to `tests/site.smoke.sh`, each following the existing trap-and-restore pattern so the promoted content trees are restored even on failure. `technical/`, `roadmap/`, `devlog/` and `pages/` are read-only promote targets; leaving one dirty is itself a failure condition, so assert cleanliness after each case.

  - D-39 unresolvable wikilink: temporarily append a bracketed link to a non-existent target inside one technical document, confirm the build exits non-zero and that the error output names both the file and the link text, then restore.
  - D-30 draft leak: temporarily add draft frontmatter to one technical document, rebuild, and confirm it appears in neither the milestone index, the full technical index, nor its milestone announcement's generated deep-dive list — all three, because the generated chrome is the case most easily forgotten. Restore afterwards.
  - Deep-dive resolution failure: temporarily rename or renumber one technical filename so a roadmap phase entry can no longer resolve, confirm the build exits non-zero naming the offending roadmap file, then restore.

Each case asserts `git status --porcelain` is empty for the affected tree once restored.
  </action>
  <verify>
    <automated>bash tests/site.smoke.sh && git status --porcelain devlog/ technical/ roadmap/ pages/ | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - The unresolvable-link case fails the build and the captured output contains both the file path and the bracketed link text
    - The draft case produces zero occurrences of the drafted document's slug across the built technical indexes and its milestone announcement page
    - The unresolvable deep-dive case fails the build naming the roadmap file
    - After the whole script runs, `git status --porcelain devlog/ technical/ roadmap/ pages/` is empty
    - `bash tests/site.smoke.sh` exits 0 overall
  </acceptance_criteria>
  <done>The three loud-fail and draft-filter behaviours that only fixtures can exercise are proven by a committed, self-restoring script.</done>
</task>

<task type="auto">
  <name>Task 3: Amend the stale requirement wording and close the phase artifacts</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/02-content-rendering-templating/02-UI-SPEC.md, .planning/phases/02-content-rendering-templating/02-VALIDATION.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the current CONT-02, CONT-04 and CONT-07 text, plus the CONT-08 v2 deferral rationale
    - `.planning/ROADMAP.md` — the Phase 2 goal line, its four success criteria, and the Plans list
    - `.planning/phases/02-content-rendering-templating/02-CONTEXT.md` — the domain section's "Scope note for the planner" and D-40/D-41, which are the authority for these amendments
    - `.planning/phases/02-content-rendering-templating/02-RESEARCH.md` — the "Scope Correction" subsection stating exactly which wording is stale
    - `.planning/phases/02-content-rendering-templating/02-05-SUMMARY.md` — the approved D-16 sentence
    - `.planning/phases/02-content-rendering-templating/02-VALIDATION.md` — specifically the Per-Task Verification Map's D-33/D-39 rows and the Manual-Only Verifications table, both of which this task rewrites
    - `tests/site.smoke.sh` — Task 2's committed fixture cases, whose real paths the D-33/D-39 rows must cite
  </read_first>
  <action>
This is the housekeeping item CONTEXT.md and RESEARCH.md both flagged rather than silently assumed. Use scoped edits; never rewrite either planning file wholesale.

`.planning/REQUIREMENTS.md`:
  - CONT-02: extend the text so it names all four content trees promoted in this phase — the announcement archive, the technical deep-dive series, the milestone roadmap detail docs, and the standalone pages — instead of the announcements alone.
  - CONT-04: replace the "standalone page mirroring the Discord pinned overview" wording with the D-36/D-37/D-38 shape: a site-voice transcribed overview plus per-milestone detail pages from a separate collection.
  - CONT-07: record the D-41 split — the build-time syntax-highlighting half landed in Phase 2; only the maths-rendering half remains deferred to v2.
  - CONT-08: keep it deferred but replace its stale rationale (the post count) with D-40's actual one — the corpus is reachable by two structural routes, so a search index still has no payoff.
  - Update the Traceability table so the amended requirements still map to Phase 2 and the coverage line stays accurate.

`.planning/ROADMAP.md`:
  - Update the Phase 2 goal line and success criteria so they describe the four trees and name the technical series, keeping the criteria in the same observable-TRUE form.
  - Fill the Plans placeholder with the eight plans of this phase and their wave grouping, and update the progress table row.

`02-UI-SPEC.md`: replace the Copywriting Contract's homepage-one-liner placeholder with the sentence approved in Plan 05, and clear the "still open" note in the developer sign-off block. Record the meta-line placement decision made in Plan 04 in the Layout &amp; Structural Contract section.

`02-VALIDATION.md`: fill in the Per-Task Verification Map's Plan and Wave columns with the real bindings — CONT-02 to plans 01/03/04/05/06, CONT-03 to 01/07, CONT-04 to 01/07, SITE-04 to 02/03/08, D-33 to 03, D-39 and D-39b to 03/06, D-34 to 03/06, D-30 to 03/08, D-41 to 03, D-42 to 04 — set each row's status, clear the Wave 0 checkboxes now that the trees exist, and set `nyquist_compliant: true` in its frontmatter.

Also reconcile that file's **Manual-Only Verifications** table with what Task 2 above actually automated. It currently carries a fourth row, "D-33 / D-39 loud-fail paths", describing a manual scratch-copy-outside-the-repo procedure. That procedure is obsolete: D-33 is proven by `tests/collections.smoke.sh`'s malformed-filename negative case (Plan 03 T2) and D-39 by `tests/lib.smoke.mjs`'s compile-level case (Plan 03 T3) plus Task 2's committed trap-and-restore fixture in `tests/site.smoke.sh`. Both run against the real content trees under trap-and-restore and assert the tree is clean afterwards, which is what removed the "would poison the promote targets" objection the row was built on. Delete that row. The table must be left with exactly three rows — rendered-text fidelity, mobile legibility, and the live post-deploy 404 — because those three are the only checks in this phase that no committed assertion makes.

Correspondingly, the D-33 and D-39 rows in the Per-Task Verification Map must name the committed fixture paths that actually ship (`tests/collections.smoke.sh`, `tests/lib.smoke.mjs`, `tests/site.smoke.sh`) rather than a manual procedure, and neither may carry a cross-reference back into the manual-only table.

Do not touch `PROJECT.md`; scope changes there are a milestone-level concern.
  </action>
  <verify>
    <automated>grep -q 'technical' .planning/REQUIREMENTS.md && grep -q '02-01-PLAN.md' .planning/ROADMAP.md && grep -q 'nyquist_compliant: true' .planning/phases/02-content-rendering-templating/02-VALIDATION.md && npm test</automated>
  </verify>
  <acceptance_criteria>
    - CONT-02's text in `.planning/REQUIREMENTS.md` names the technical and roadmap detail trees
    - CONT-04's text describes an overview plus per-milestone detail pages
    - CONT-07's entry records the D-41 split rather than a whole-requirement v2 deferral
    - CONT-08's deferral rationale cites the structural-reachability argument, not the post count
    - `.planning/ROADMAP.md`'s Phase 2 section lists all eight plan filenames grouped by wave and its progress-table row is updated
    - `02-VALIDATION.md` has no `TBD` left in the Plan or Wave columns and its frontmatter sets `nyquist_compliant: true`
    - `02-VALIDATION.md`'s Manual-Only Verifications table has exactly three data rows — rendered-text fidelity, mobile legibility, and the live post-deploy 404 — and the D-33 / D-39 loud-fail row is gone
    - No row in that table describes a scratch-copy procedure, and the file contains no claim that D-33 or D-39 is verified manually
    - The Per-Task Verification Map's D-33 and D-39 rows name `tests/collections.smoke.sh`, `tests/lib.smoke.mjs` and `tests/site.smoke.sh` as their proof, matching the fixtures Task 2 committed
    - `02-UI-SPEC.md` contains the approved homepage sentence and no longer describes it as open
    - `npm test` still exits 0
  </acceptance_criteria>
  <done>Requirement and roadmap wording matches the phase's real scope, and every phase artifact is closed out.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — text fidelity, responsive legibility, live 404</name>
  <files>(none — verification only)</files>
  <action>Complete Tasks 1–3 first, then run the three manual verifications below and block on the developer's verdict. Record each outcome in the plan summary, including a negative live-404 result, which is a real finding for Phase 4 rather than a failure to hide.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - All three manual checks are performed and their outcomes recorded verbatim in the summary
    - Any failure names the specific check and page
  </acceptance_criteria>
  <done>The three manual-only verifications from 02-VALIDATION.md are performed and their results recorded.</done>
  <what-built>
The complete Phase 2 site: the announcement archive at the site root with nine posts, the technical index and 55 deep-dives with highlighted code and tables of contents, the roadmap overview with eight milestone detail pages, the How It's Made page, a custom 404, a favicon, canonical URLs and a sitemap. All automated checks in Plans 01–08 pass.

Three verifications remain that no assertion can honestly make — 02-VALIDATION.md's Manual-Only table.
  </what-built>
  <how-to-verify>
1. **Rendered text fidelity (CONT-02, D-14).** Open `devlog/2026-06-05-a-moon-that-actually-orbits.md` and its page at `/devlog/2026-06-05-a-moon-that-actually-orbits/` side by side. Then do the same for `technical/m0.1/phase-01-window-surface.md` and its page. Confirm: no duplicated title, no site-injected heading, blockquotes and lists and code fences all present and unrestyled, and the prose reading identically to the source.

2. **Mobile and desktop legibility (SITE-04).** Run `npm run dev` and load the site root, one technical deep-dive, and `/roadmap/` at 375px and at 1440px. Confirm: the header nav wraps to a second row rather than overflowing at 375px; the table of contents is a collapsible disclosure below 640px and a sticky sidebar above it; no horizontal scrolling anywhere except inside code fences, which should scroll horizontally by design; and the reading column stays at a comfortable measure on the wide viewport.

3. **Live 404 under the Pages base path (SITE-04).** This one requires the deploy to have run. After this phase's commits are on `main` and the Actions run has finished, fetch a deliberately non-existent URL under `https://spoods-studios.github.io/interstellar-website/` — for example `.../interstellar-website/definitely-not-a-page/`. Confirm the site's own 404 page renders with the site header and footer, not GitHub's generic 404. This is the empirical resolution of 02-RESEARCH.md Pitfall 6, where docs were silent and search results conflicted; record the outcome either way, because a negative result is a real finding for Phase 4's deploy hardening.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed, naming which of the three checks and on which page.</resume-signal>
</task>

</tasks>

<threat_model>
> Scope: OWASP ASVS Level 1. This is a build-time static-site pipeline with no runtime server, no authentication, no user input and no persisted state; L1 is the applicable tier.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built artifact → GitHub Pages | Whatever the build emits is what the public gets; nothing filters it afterwards |
| content tree → build | Fixture-based negative testing temporarily writes into read-only promote targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-15 | Tampering | Negative-test fixtures left behind in a read-only content tree | high | mitigate | Every fixture case uses the established trap-and-restore pattern and asserts an empty `git status --porcelain` for the affected tree afterwards (Task 2) |
| T-02-16 | Information Disclosure | Draft entry reaching the published artifact | medium | mitigate | Task 2's D-30 case proves absence from indexes AND generated cross-link chrome, which is the path 02-RESEARCH.md's Security Domain flags as easiest to miss |
| T-02-17 | Denial of Service | Dead internal links after cross-plan integration | medium | mitigate | The whole-site dead-link sweep in Task 1 is the integration check that no individual slice plan could perform |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan; the phase's only dependency was audited and installed in Plan 03 |
</threat_model>

<verification>
- `npm test` exits 0 across every committed harness
- Zero dead internal links, zero `<script>` tags, complete canonical and sitemap coverage
- All content trees clean in `git status` after the fixture runs
- All four ROADMAP Phase 2 success criteria confirmed TRUE
- Human verification of text fidelity, responsive legibility, and the live 404 recorded
</verification>

<success_criteria>
Every Phase 2 success criterion is demonstrably true, the loud-fail and draft-filter guarantees are proven rather than assumed, the documentation matches the delivered scope, and the three human-only verifications are recorded.
</success_criteria>

<output>
Create `.planning/phases/02-content-rendering-templating/02-08-SUMMARY.md` when done. Record the live 404 outcome explicitly — Phase 4's deploy hardening and CONT-06 redirect-stub work both depend on knowing it.
</output>
