---
phase: 01-stack-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - astro.config.mjs
  - tsconfig.json
  - .gitignore
  - src/content.config.ts
  - src/pages/index.astro
autonomous: false
requirements: [SITE-02, CONT-01]
user_setup: []

must_haves:
  truths:
    - "`npm run build` exits 0 and emits dist/index.html listing the manifesto title + its date"
    - "The frontmatter-less manifesto ingests with date 2026-04-07 and slug why-im-building-a-hyperrealistic-space-sim derived from its filename (CONT-01, D-01)"
    - "The site domain + base path exist in exactly one file (astro.config.mjs); no github.io/base literal appears under src/ (SITE-02)"
    - "A devlog *.md file with neither frontmatter nor a parseable YYYY-MM-DD-slug.md name fails the build loudly, naming the offending file (D-10)"
  artifacts:
    - path: "astro.config.mjs"
      provides: "The single URL config location — site + base keys (SITE-02, D-06)"
      contains: "base"
    - path: "src/content.config.ts"
      provides: "devlog collection: glob loader reading ./devlog, all-optional Zod schema, filename-fallback generateId, loud-fail + non-empty assertion"
      exports: ["collections"]
    - path: "src/pages/index.astro"
      provides: "Throwaway proof list: getCollection('devlog') rendered as title + date, title-from-H1 fallback, base-path-safe links (D-07)"
      min_lines: 15
    - path: "package.json"
      provides: "Astro dependency (caret range) + committed lockfile (D-04, D-05)"
      contains: "astro"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/content.config.ts"
      via: "getCollection('devlog') consumes the devlog collection"
      pattern: "getCollection\\(['\"]devlog['\"]\\)"
    - from: "src/content.config.ts"
      to: "devlog/"
      via: "glob loader base points at the repo-root devlog directory"
      pattern: "glob\\("
    - from: "src/pages/index.astro"
      to: "astro.config.mjs"
      via: "base-path links resolve through import.meta.env.BASE_URL, never a hardcoded string"
      pattern: "import\\.meta\\.env\\.BASE_URL"
---

<objective>
Stand up the Astro project at the repo root and prove the ingest → build path end-to-end locally: the frontmatter-less manifesto in `devlog/` is ingested by a permissive Content Layer collection and rendered as a title + date list on a throwaway page that `npm run build` emits to `dist/`.

This is the local half of the Phase 1 walking skeleton (deploy is Plan 02). It delivers the smallest observable full-stack capability: run the build, open `dist/index.html`, and see the manifesto's title and `2026-04-07` date rendered live from Markdown.

Purpose: Establishes the config-driven URL (SITE-02), the permissive filename-fallback ingestion (CONT-01, D-01/D-02), and the loud-fail-on-malformed-content guard (D-10) that every later phase builds on. Boring is correct — npm, caret ranges, minimal manual scaffold, no test framework (D-03/D-04/D-05).
Output: `package.json` + committed `package-lock.json`, `astro.config.mjs` (the ONE URL config location), `tsconfig.json`, `.gitignore`, `src/content.config.ts`, `src/pages/index.astro`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stack-scaffolding/01-CONTEXT.md
@.planning/phases/01-stack-scaffolding/01-RESEARCH.md
@.planning/phases/01-stack-scaffolding/01-PATTERNS.md
@.planning/phases/01-stack-scaffolding/01-UI-SPEC.md
@.planning/phases/01-stack-scaffolding/SKELETON.md
@CLAUDE.md
@devlog/2026-04-07-why-im-building-a-hyperrealistic-space-sim.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify astro + typescript package legitimacy before install (T-01-SC)</name>
  <read_first>
    - .planning/phases/01-stack-scaffolding/01-RESEARCH.md  (## Package Legitimacy Audit — both packages flagged SUS "too-new", assessed false-positive)
  </read_first>
  <action>Halt before any `npm install` runs and require a human supply-chain confirmation for `astro` and `typescript`. Present the two npmjs.com package pages (below) for verification and do not proceed to Task 2's install until the human approves. This gate is never auto-approved, even under auto-advance / yolo mode (T-01-SC).</action>
  <what-built>
    Nothing yet — this is the mandatory pre-install supply-chain gate. The next task runs `npm install astro` (and optionally `typescript`). The Package Legitimacy Audit flagged both as SUS ("too-new"), assessed as a false positive (routine recent release of established packages: astro ~3.9M weekly downloads / withastro org; typescript ~226M weekly downloads / Microsoft). Per the supply-chain protocol this still requires a one-line human confirmation before install — it is never auto-approvable, even in yolo/auto-advance mode.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/astro — confirm publisher is the `withastro` org, repo is github.com/withastro/astro, weekly downloads are in the millions.
    2. Open https://www.npmjs.com/package/typescript — confirm publisher is Microsoft, repo is github.com/microsoft/TypeScript, weekly downloads are in the hundreds of millions.
    3. Confirm neither is a typosquat of the intended name (exact spelling `astro`, `typescript`).
  </how-to-verify>
  <acceptance_criteria>
    - Human has confirmed both packages resolve to their official publisher/repo on npmjs.com.
    - No install has run before this confirmation.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to install, or name a concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold Astro at repo root — manual install + config-driven URL (D-03/D-04/D-05/D-06, SITE-02)</name>
  <files>package.json, package-lock.json, astro.config.mjs, tsconfig.json, .gitignore</files>
  <read_first>
    - .planning/phases/01-stack-scaffolding/01-RESEARCH.md  (## Standard Stack "Installation (manual)"; Pitfall 1; "astro.config.mjs" code example; Environment Availability)
    - .planning/phases/01-stack-scaffolding/01-PATTERNS.md  ("package.json / scaffold install", "astro.config.mjs", "tsconfig.json" sections)
    - .planning/phases/01-stack-scaffolding/01-CONTEXT.md    (D-03 minimal manual scaffold, D-04 npm/lockfile, D-05 caret+npm ci, D-06 repo root)
    - CLAUDE.md  (repo-root untouchability of devlog/, stack facts)
  </read_first>
  <action>
    Scaffold Astro MANUALLY at the repo root — do NOT run `npm create astro@latest .` (Pitfall 1: the root already holds CLAUDE.md/devlog/vault/.planning and the CLI aborts on a non-empty directory; manual install also matches D-03's "wired manually" intent). Run `npm init --yes`, then `npm install astro` and `npm install --save-dev typescript` (TypeScript is the discretion item A4 — install it so tsconfig type-checking works). D-05: keep the caret ranges npm writes by default (no exact pins); D-04: commit the resulting `package-lock.json`.

    Write `astro.config.mjs` as the ONLY file allowed to contain the deployment domain and base path (SITE-02, D-06): export a defineConfig with `site` set to the spoods-studios GitHub Pages origin and `base` set to the repository base path — copy BOTH exact string values verbatim from 01-RESEARCH.md's "astro.config.mjs (SITE-02 — the one config location)" code example (target URL already confirmed this session by enabling GitHub Pages via the GitHub API). These two strings must live here and nowhere else.

    Write `tsconfig.json` extending `astro/tsconfigs/base` (the documented minimal Astro setup). Write `.gitignore` ignoring `node_modules/`, `dist/`, and `.astro/` (discretion item; standard Astro defaults, D-06 keeps everything at root). Do NOT create a `site/` subdirectory and do NOT add path inputs anywhere. Do NOT edit anything under `devlog/` (D-01).
  </action>
  <verify>
    <automated>test -f astro.config.mjs && test -f package-lock.json && node -e "const p=require('./package.json');if(!/^\^/.test(p.dependencies.astro))throw new Error('astro not a caret range')" && grep -q "spoods-studios.github.io" astro.config.mjs && grep -q "/interstellar-website" astro.config.mjs && npx astro info</automated>
  </verify>
  <acceptance_criteria>
    - `astro.config.mjs` exists and contains both the github.io origin and the `/interstellar-website` base; these two strings appear in NO other file created this plan.
    - `package.json` lists `astro` as a caret range (`^`), not an exact pin; `package-lock.json` exists and is staged for commit.
    - `tsconfig.json` extends `astro/tsconfigs/base`; `.gitignore` lists `node_modules/`, `dist/`, `.astro/`.
    - `npm create astro` was NOT used (no scaffold prompt artifacts); no `site/` subdirectory exists; `git status` shows `devlog/` unchanged.
  </acceptance_criteria>
  <done>Astro is installed at the repo root with a caret-ranged dependency and committed lockfile; `astro.config.mjs` holds the sole copy of the site/base URL; `npx astro info` runs cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ingest → render slice — permissive collection + throwaway proof page (CONT-01, D-01/D-02/D-07/D-10)</name>
  <files>src/content.config.ts, src/pages/index.astro</files>
  <read_first>
    - .planning/phases/01-stack-scaffolding/01-RESEARCH.md  (Pattern 1 external-path collection + all-optional schema; Pattern 2 base-path-safe links; Pitfall 2 silent-empty-collection; Pitfall 3 hardcoded base; Assumption A2 loud-fail-on-build)
    - .planning/phases/01-stack-scaffolding/01-PATTERNS.md  ("src/content.config.ts" + "src/pages/index.astro" pattern assignments, incl. the two required deviations)
    - .planning/phases/01-stack-scaffolding/01-UI-SPEC.md   (## Copywriting Contract — verbatim page copy)
    - .planning/phases/01-stack-scaffolding/01-CONTEXT.md    (D-01 filename fallback/title-from-H1, D-02 all-optional schema, D-07 throwaway list, D-10 loud-fail)
    - devlog/2026-04-07-why-im-building-a-hyperrealistic-space-sim.md  (the frontmatter-less acceptance fixture — first line is the H1 title; DO NOT edit, D-01)
    - ../studio/vault/devlog/_TEMPLATE.md  (the full frontmatter field set the schema mirrors, all-optional)
  </read_first>
  <behavior>
    - Given the frontmatter-less manifesto `devlog/2026-04-07-why-im-building-a-hyperrealistic-space-sim.md`, the `devlog` collection ingests it with slug `why-im-building-a-hyperrealistic-space-sim` and date `2026-04-07`, both derived from the filename (no frontmatter present).
    - Given no frontmatter `title`, the rendered list item title falls back to the manifesto's first `# H1`: `Why I'm Building a Hyperrealistic Space Sim from Scratch` (D-01).
    - `npm run build` exits 0 and `dist/index.html` contains an `<h1>Devblog</h1>`, `<title>Interstellar Engine — Devblog</title>`, and a list item rendering `{title} — 2026-04-07`.
    - Given a devlog `*.md` whose name has neither valid frontmatter nor a parseable `YYYY-MM-DD-slug.md` date, `npm run build` exits NON-ZERO with an error message naming that file (D-10).
    - Given a misconfigured loader `base` that resolves to zero entries, the build fails loudly via an explicit non-empty assertion rather than silently rendering an empty list (Pitfall 2).
  </behavior>
  <action>
    Write `src/content.config.ts` defining a single `devlog` collection using Astro's Content Layer `glob()` loader (NOT legacy `src/content/config.ts` — Content Layer API, Astro 6+). Base the loader implementation on 01-PATTERNS.md's `src/content.config.ts` assignment and 01-RESEARCH.md Pattern 1: loader `base` resolves to the repo-root `devlog/` directory (relative to `src/`, i.e. one level up), pattern matches `**/*.md` and excludes any `_TEMPLATE.md` (D-10 corollary — non-post files get an explicit exclusion, not silent tolerance). Implement `generateId` to parse the filename against the `YYYY-MM-DD-slug.md` shape and return the slug segment; on no match, `throw` an Error whose message NAMES the offending file (D-10 loud-fail). Define the Zod `schema` mirroring the studio `_TEMPLATE.md` field set — `milestone`, `title`, `date`, `status`, `discord_post_id`, `audience`, `hero_visual` — with EVERY field `.optional()` (D-02: validate-when-present, require-nothing; `date` via `z.coerce.date()`, `status` as an enum of `draft`/`published`). Export `collections = { devlog }`.

    Write `src/pages/index.astro` as the throwaway proof page (D-07, ~15-20 lines, replaced wholesale in Phase 2). Query `getCollection('devlog')`. Immediately after the query, add the Pitfall-2 guard: if the collection has zero entries, `throw` an Error (the loader base misconfigures silently, so this assertion is what makes D-10 hold for the empty-collection case). For each entry resolve a display title as frontmatter `title` when present else the entry's first `# H1` from its body (D-01 title-from-H1 fallback; render the body's markdown headings source or the collection's rendered heading — read 01-PATTERNS.md deviation note 1), and resolve a display date as frontmatter `date` else the filename-derived date, formatted `YYYY-MM-DD` (no locale formatting this phase, per UI-SPEC). Render exactly the UI-SPEC copy: `<title>` = `Interstellar Engine — Devblog`, page `<h1>` = `Devblog`, and a `<ul>` where each `<li>` is `{title} — {date}`. Ship NO custom CSS (D-07 unstyled is deliberate). For any internal link or asset reference, resolve the path through `import.meta.env.BASE_URL` — never write the deployment domain or base path as a literal in this file (SITE-02, Pitfall 3); the sole copy of those strings stays in `astro.config.mjs`.
  </action>
  <verify>
    <automated>npm run build && grep -q "Devblog" dist/index.html && grep -q "Why I'm Building a Hyperrealistic Space Sim from Scratch" dist/index.html && grep -q "2026-04-07" dist/index.html && ! grep -rIn -e 'github\.io' -e '/interstellar-website' src/</automated>
    <automated>MARKER="zzz-not-a-post.md"; printf 'garbage body no h1\n' > "devlog/$MARKER"; if npm run build > /tmp/gsd-d10.log 2>&1; then rm -f "devlog/$MARKER"; echo "D-10 FAIL: build did not error on malformed file"; exit 1; fi; grep -q "$MARKER" /tmp/gsd-d10.log && rm -f "devlog/$MARKER" && git status --porcelain devlog/ | grep -q . && { echo "D-10 FAIL: devlog/ left dirty"; exit 1; } || true; git status --porcelain devlog/ | grep -q . && exit 1 || echo "D-10 OK: build failed loudly naming $MARKER; devlog/ clean"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; `dist/index.html` contains `Devblog`, the manifesto H1 title, and `2026-04-07`.
    - `grep -rIn` for the github.io domain or `/interstellar-website` base under `src/` returns nothing (SITE-02 clean).
    - Adding a bad-named, frontmatter-less `.md` to `devlog/` makes `npm run build` exit non-zero with the offending filename in the error output; after the one-off check, `git status` shows `devlog/` clean (D-01 preserved).
    - `src/content.config.ts` schema has all seven fields `.optional()` and excludes `_TEMPLATE.md`; `src/pages/index.astro` contains the explicit zero-entry `throw` (Pitfall 2 guard).
  </acceptance_criteria>
  <done>The manifesto renders as a title + date list item that `npm run build` bakes into `dist/index.html`; malformed content fails the build loudly by name; no URL literal leaks outside `astro.config.mjs`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev machine | Third-party package code (`astro`, `typescript`) enters the build at install time |
| `devlog/*.md` (git content) → build | Author-controlled Markdown crosses into the Content Layer; git-trusted, but shape is not guaranteed (frontmatter optional) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install astro` / `typescript` (supply chain) | high | mitigate | Task 1 blocking-human legitimacy checkpoint before any install; both packages verified to official publisher/repo on npmjs.com; caret ranges + committed lockfile so CI reinstalls the exact resolved tree (`npm ci`, Plan 02) |
| T-01-02 | Tampering / Information Disclosure | `src/content.config.ts` all-optional schema silently accepting malformed content | medium | mitigate | D-10 loud-fail: `generateId` throws naming the offending file on an unparseable name; explicit zero-entry assertion in `index.astro` guards the silent-empty-collection failure mode (Pitfall 2). Negative smoke test in Task 3 verify confirms non-zero exit. |
| T-01-03 | Information Disclosure | Hardcoded deployment URL/base leaking into templates (couples config to code) | low | mitigate | SITE-02: single URL source in `astro.config.mjs`; base-path links resolve via `import.meta.env.BASE_URL`; Task 3 verify negative-greps `src/` for URL literals |

ASVS L1, block-on-high. T-01-SC (high) is mitigated by the mandatory blocking-human gate — the plan is non-autonomous specifically to satisfy this.
</threat_model>

<verification>
- `npm run build` exits 0 and emits `dist/index.html`.
- `dist/index.html` contains `Devblog` (h1), the manifesto H1 title, and `2026-04-07`.
- `grep -rIn` for the github.io domain / `/interstellar-website` base under `src/` returns zero hits (the strings live only in `astro.config.mjs`).
- The Task 3 D-10 negative one-off: a bad-named devlog file forces a non-zero build naming the file, and leaves `devlog/` clean afterward.
- `git status` shows nothing under `devlog/` modified (D-01 untouchability).
</verification>

<success_criteria>
- Astro is installed at the repo root via manual install (not `create-astro`), with a caret-ranged `astro` dependency and a committed `package-lock.json`.
- The site domain and base path exist in exactly one file (`astro.config.mjs`); Phase-1 success criterion 3 (config-driven URL, no hardcoded strings) holds for all `src/` code.
- The frontmatter-less manifesto is ingested and rendered with a filename-derived date and an H1-derived title (Phase-1 success criterion 4 / CONT-01 / D-01).
- Malformed content fails the build loudly, naming the file (D-10 slice of SITE-03 pulled forward).
</success_criteria>

## Artifacts this phase produces

New files (none pre-exist — greenfield repo, API surface has 0 symbols):
- `package.json`, `package-lock.json` — npm project + committed lockfile (D-04/D-05)
- `astro.config.mjs` — exports `defineConfig({ site, base })`; the sole URL config location (SITE-02)
- `tsconfig.json` — extends `astro/tsconfigs/base`
- `.gitignore` — ignores `node_modules/`, `dist/`, `.astro/`
- `src/content.config.ts` — exports `collections = { devlog }`; defines the `devlog` collection, the filename-fallback `generateId`, the `YYYY-MM-DD-slug.md` filename regex, and the all-optional Zod schema (fields: `milestone`, `title`, `date`, `status`, `discord_post_id`, `audience`, `hero_visual`)
- `src/pages/index.astro` — the throwaway proof page; consumes `getCollection('devlog')`, holds the zero-entry assertion and the title-from-H1 fallback

New symbols / identifiers: `collections`, `devlog` (collection), `generateId` (loader fn), the filename regex, the `site`/`base` config keys, and the Astro-default npm scripts (`dev`, `build`, `preview`).

<output>
Create `.planning/phases/01-stack-scaffolding/01-01-SUMMARY.md` when done.
</output>