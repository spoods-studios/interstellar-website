---
phase: 04-analytics-launch-content-deploy-hardening
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - devlog/2026-07-30-first-burn.md
  - assets/m1.1-hero-first-burn.png
  - technical/m1.1/phase-48-rigid-body-attitude-kernel.md
  - technical/m1.1/phase-49-tsiolkovsky-thrust-fuel-kernel.md
  - technical/m1.1/phase-50-rcs-mixer-kernel.md
  - technical/m1.1/phase-51-n-body-integration-hookup.md
  - technical/m1.1/phase-52-rotational-integration-input-channel.md
  - technical/m1.1/phase-53-telemetry-debug-hud.md
  - technical/m1.1/phase-53.1-demo-control-policy-attitude-indicator.md
  - technical/m1.1/phase-53.2-trail-interpolation-accelerated-time.md
  - technical/m1.1/phase-53.3-camera-stability-refly.md
  - technical/m1.1/phase-54-findings-and-fixes.md
  - roadmap/M1.1.md
  - pages/roadmap.md
  - tests/collections.smoke.sh
  - tests/build.smoke.sh
  - tests/technical.smoke.sh
  - tests/roadmap.smoke.sh
autonomous: false
requirements: [CONT-05]

must_haves:
  truths:
    - "The M1.1 launch post is the first entry on the archive homepage and the first item in the RSS feed, by date ordering alone with no route or query change"
    - "All ten M1.1 deep-dives render at their own URLs, including the three decimal-numbered phases, and appear in both the technical full index and the m1.1 milestone index"
    - "The M1.1 roadmap detail page renders with its deep-dive placeholders resolved into real links, and the roadmap overview links to it"
    - "The launch post's hero image resolves to a built asset and becomes its OpenGraph card"
    - "No promoted markdown file differs by a single byte from its studio-vault source"
    - "The roadmap overview and the M1.1 detail page both present M1.1 as closed — the site never shows M1.1 in progress while its launch post tops the archive"
    - "Zero occurrences of the Discord-era deep-dive placeholder wording survive anywhere in the built site"
  prohibitions:
    - statement: "MUST NOT alter, reformat, paraphrase, re-wrap or otherwise fix any promoted prose while copying it — the site layer renders studio-authored, VOICE-locked content and is never an editor of it"
      status: unverified
    - statement: "MUST NOT publish studio content the author has marked unpublished — a document still awaiting its VOICE pass must not reach the site, because publishing it ships words the author has not approved"
      status: unverified
  artifacts:
    - path: "devlog/2026-07-30-first-burn.md"
      provides: "the M1.1 launch announcement"
      contains: "milestone: M1.1"
    - path: "assets/m1.1-hero-first-burn.png"
      provides: "launch post hero image, picked up by the eager assets glob"
    - path: "roadmap/M1.1.md"
      provides: "M1.1 milestone detail page source"
      contains: "Era 1"
    - path: "pages/roadmap.md"
      provides: "refreshed site-voice roadmap overview transcription per D-64"
  key_links:
    - from: "roadmap/M1.1.md"
      to: "technical/m1.1/"
      via: "deep-dive placeholders resolved by resolvePhase at config-load time"
      pattern: "technical/m1\\.1/phase-"
    - from: "devlog/2026-07-30-first-burn.md"
      to: "assets/m1.1-hero-first-burn.png"
      via: "body image reference reaching heroFor() through entry.assetImports"
      pattern: "assets/m1\\.1-hero-first-burn\\.png"
---

<objective>
Promote the full M1.1 content tree so the launch post is live at the top of the
archive and the feed — the milestone the whole roadmap aimed at.

Purpose: CONT-05 and the project's Core Value ("the archive + launch post must be
live at M1.1 close"). Everything needed to render this content already exists:
date ordering puts 2026-07-30 first in both the archive and the feed,
parsePhaseNumber already handles decimal phases, milestoneSortKey already sorts
m1.1 after every m0.x, and the hero pipeline picks up a new PNG through the eager
assets glob with no code change. The work is a faithful promote plus the test
updates that keep the corpus assertions honest.

Output: 13 promoted files, a refreshed roadmap overview, and corpus assertions
that describe the new corpus instead of the old one.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analytics-launch-content-deploy-hardening/04-CONTEXT.md
@.planning/phases/04-analytics-launch-content-deploy-hardening/04-RESEARCH.md
@.planning/phases/04-analytics-launch-content-deploy-hardening/04-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Fix the launch post's permanent URL slug</name>
  <decision>The filename — and therefore the permanent public URL — of the M1.1 launch post in devlog/.</decision>
  <context>
    This phase's own CONT-06 work exists because published post URLs are pinned by
    Discord embeds, RSS guids and studio-vault references. Once this post deploys
    and is shared, its URL cannot change without a redirect stub. The redirect
    demonstration entry in Plan 04-04 and the CI probe URL set in Plan 04-05 both
    reference whatever is chosen here, so it is settled once, now.

    Established convention across all nine promoted posts: the repo filename is the
    publication date plus the post's title slugified — never the studio milestone
    filename. The studio draft m0.8-perturbations.md (title "Making Mercury
    Precess") landed as 2026-07-13-making-mercury-precess.md. The M1.1 draft is
    m1.1-spacecraft-control.md, dated 2026-07-30, titled "First Burn".
  </context>
  <options>
    <option id="convention">
      <name>2026-07-30-first-burn.md</name>
      <pros>The mechanical result of the existing nine-post convention (date plus slugified title). Matches the announcement headline. Short and stable.</pros>
      <cons>"first-burn" is generic in isolation — out of context it does not say which milestone it belongs to.</cons>
    </option>
    <option id="milestone-named">
      <name>2026-07-30-spacecraft-control.md</name>
      <pros>Self-describing about the milestone; matches the studio draft filename and the roadmap milestone name.</pros>
      <cons>Breaks the title-derived-slug convention the other nine posts follow, so archive URL shapes stop being predictable from titles.</cons>
    </option>
  </options>
  <resume-signal>Select: convention, milestone-named, or supply an exact filename</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — retarget the corpus assertions at the post-promote corpus</name>
  <files>tests/collections.smoke.sh, tests/build.smoke.sh, tests/technical.smoke.sh, tests/roadmap.smoke.sh</files>
  <read_first>
    - `tests/collections.smoke.sh` lines 17-28 — the four exact collection counts (devlog 9, technical 56, roadmap 8, pages 2) and the find path glob scoped to the m0 milestone series, which silently excludes any other milestone.
    - `tests/build.smoke.sh` lines 20-42 — the nine-archive-link count, the newest-first byte-offset ordering check that names the current newest post's title, and the milestone-tag spot checks.
    - `tests/technical.smoke.sh` lines 12-18 and 60-70 — the 55-deep-dive find glob, the 55-href full-index count whose regex is anchored on the m0 series, and the per-milestone index loop.
    - `tests/roadmap.smoke.sh` lines 12-16, 30-34 and 48-60 — the two loops over the m0 milestones, the overview's exactly-eight milestone-link count whose regex is anchored on the m0 series, the negative assertion that the overview must not link to an M1.1 detail page, and the assertion that the overview presents M1.1 as in progress.
    - `.planning/phases/04-analytics-launch-content-deploy-hardening/04-RESEARCH.md` — Pitfall 3, which enumerates the count changes but does not cover the two roadmap-overview assertions that must invert rather than increment.
  </read_first>
  <behavior>
    - Against the pre-promote corpus the suite is red at exactly these corpus assertions and nowhere else.
    - After the promote lands, every one of them passes.
    - No assertion silently under-asserts: a milestone-scoped glob that would ignore an entire new milestone is widened, not left alone.
  </behavior>
  <action>
    Update the four harness files so their corpus expectations describe the
    post-promote corpus. New totals: devlog 10, technical 66 (65 deep-dives plus
    the how-to-read legend), roadmap 9, pages 2 — the M1.1 tree adds one
    announcement, ten deep-dives and one roadmap detail document.

    In tests/collections.smoke.sh, raise the four expected counts and widen the
    technical find path glob so it matches every milestone directory rather than
    only the m0 series. A milestone-anchored glob keeps passing while ignoring the
    whole new milestone, which is a silent under-assertion, not a pass.

    In tests/build.smoke.sh, raise the archive-link count to ten and retarget the
    newest-first ordering check: the newest post is now the M1.1 launch post, so
    the byte-offset comparison must use its title against the manifesto's rather
    than the previous newest post's title. Add an M1.1 milestone-tag spot check
    beside the existing M0.8 and M0.1 ones.

    In tests/technical.smoke.sh, raise the deep-dive count to 65 (66 pages
    including the legend), widen both the find path glob and the full-index href
    regex so they match the m1.1 directory as well as the m0 series, add m1.1 to
    the per-milestone index loop, and add an existence spot check for at least one
    of the three decimal-numbered M1.1 phase pages — the decimal case is what
    parsePhaseNumber and resolvePhase are being trusted to handle.

    In tests/roadmap.smoke.sh, make four changes. Add m1.1 to both loops (the
    detail-page existence loop and the per-page technical-anchor loop). Raise the
    overview's milestone-link count to nine and widen its href regex off the
    m0-only anchor. Invert the negative assertion that currently fails the suite
    when the overview links to an M1.1 detail page — after the promote that link is
    required, because src/pages/roadmap/index.astro generates one link per visible
    roadmap collection entry. Finally, replace the assertion that the overview
    presents M1.1 as in progress with one that asserts the overview presents M1.1
    as closed and names M1.2 as what is next, matching the refreshed transcription
    Task 3 produces.

    Do not touch tests/distribution.smoke.sh: its checks are relative
    (baseline versus mutated) and self-adjust to corpus size.

    Sweep for stragglers before finishing — run a recursive search for the m0
    literal across tests/ and confirm each remaining occurrence is either a
    deliberately milestone-specific named spot-check page or has been widened.
  </action>
  <verify>
    <automated>npm run build; ! bash tests/collections.smoke.sh &amp;&amp; ! bash tests/roadmap.smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - Against the pre-promote corpus, `bash tests/collections.smoke.sh` exits non-zero at the count assertions and `bash tests/roadmap.smoke.sh` exits non-zero — proving the updated assertions are load-bearing rather than vacuous.
    - `tests/collections.smoke.sh` asserts devlog 10, technical 66, roadmap 9, pages 2.
    - `grep -c 'm1.1' tests/roadmap.smoke.sh` is at least 3 (both loops plus the overview link expectation).
    - `grep -c 'm1.1' tests/technical.smoke.sh` is at least 2.
    - No `find` path glob or href regex in the four files is still anchored so that it can match only the m0 milestone series.
    - No assertion in the four files still requires the overview to omit an M1.1 detail link, and none still requires the overview to describe M1.1 as in progress.
  </acceptance_criteria>
  <done>The corpus assertions describe the post-promote corpus and demonstrably fail against the pre-promote one.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh the studio-side roadmap transcription against the closed-M1.1 pin</name>
  <files>../studio/vault/devlog/drafts/roadmap.md, ../studio/vault/project/roadmap-detail/M1.1.md</files>
  <read_first>
    - `../studio/vault/devlog/discord/roadmap-overview.pinned.md` — the updated pinned overview, the authoritative source text: M1.1 shown closed with its one-line summary and phase count, M1.2 shown as next with its scope.
    - `pages/roadmap.md` — the current site-voice transcription in this repo. This is the register and structure to match: frontmatter with title/updated/status, an H1, an arc paragraph, a reading-guide paragraph, then one section per era with a bullet per milestone. Its Era 1 section still presents M1.1 as in progress.
    - `../studio/vault/devlog/VOICE.md` — the locked voice the transcription sits inside.
    - `../studio/vault/project/roadmap-detail/M1.1.md` lines 1-25 — the milestone summary block whose status prose and phase-count line are stale relative to the pin.
    - `roadmap/M0.8.md` lines 1-20 — the shape a closed milestone's detail-document summary block takes: opened and closed dates, era line, phase count.
    - `../studio/vault/decisions/Decision Log.md` — the cross-repo decision log; D-25/D-56 are the precedent for writing to the studio vault from this repo's phase work.
  </read_first>
  <action>
    Per D-64 the roadmap overview transcription is rewritten studio-side and
    promoted with the tree. Write the refreshed site-voice transcription into the
    studio vault at the drafts path the promote flow reads from, so the vault stays
    the source of truth and this repo's copy is downstream of it (the D-25/D-56
    cross-repo precedent). Keep the existing transcription's structure, frontmatter
    keys and register; change only what the pin changed — M1.1 moves from in
    progress to closed with its real one-line summary and phase count, and M1.2 is
    added as the next milestone with its scope. Set the frontmatter updated date to
    the promote date. Do not import the pin's Discord channel references, its "how
    this channel works" preamble, or any status marker that has no site equivalent;
    the existing transcription already establishes how each of those is handled.

    Also correct the two stale status statements in the studio's M1.1 milestone
    detail document: the summary paragraph describing the milestone as still in
    progress awaiting its review gate, and the phase-count line describing the final
    phase as pending and excluded. Both contradict the pin and the launch post.
    Scope this edit tightly to those two statements — do not add, remove or renumber
    any phase section, do not touch any deep-dive placeholder line, and do not
    restyle surrounding prose. This is an extension of D-64's stated rationale (the
    site must not show M1.1 in progress while its launch post tops the archive) to
    the other page that would otherwise say exactly that; it is recorded in this
    plan's Flagged Assumptions section for review.

    Commit these files in the studio repository as its own conventional commit,
    following the Plan 03-06 precedent: the studio repo is written to and committed
    but deliberately left unpushed.
  </action>
  <verify>
    <automated>grep -q 'M1.2' ../studio/vault/devlog/drafts/roadmap.md &amp;&amp; ! grep -qiE 'M1\.1[^\n]*in progress' ../studio/vault/devlog/drafts/roadmap.md &amp;&amp; ! grep -qi 'still pending and not included below' ../studio/vault/project/roadmap-detail/M1.1.md &amp;&amp; git -C ../studio status --porcelain | grep -qv . </automated>
  </verify>
  <acceptance_criteria>
    - `../studio/vault/devlog/drafts/roadmap.md` exists, carries frontmatter with title, updated and status keys, and its Era 1 section names both M1.1 as closed and M1.2 as next.
    - No line in that file describes M1.1 as in progress.
    - `../studio/vault/project/roadmap-detail/M1.1.md` no longer contains the sentence fragment describing the final phase as still pending and not included, and its summary paragraph no longer describes the milestone as in progress.
    - `git -C ../studio log --oneline -1` shows a conventional commit naming this change; `git -C ../studio status --porcelain` is empty.
    - The phase-heading count in the M1.1 detail document is unchanged from before the edit, and the count of deep-dive placeholder lines is unchanged — the edit touched status prose only.
  </acceptance_criteria>
  <done>The studio vault carries a roadmap transcription and an M1.1 detail document that both describe M1.1 as closed, committed in the studio repo and left unpushed.</done>
</task>

<task type="auto">
  <name>Task 4: GREEN — atomic promote of the full M1.1 tree</name>
  <precondition>The studio vault is readable at ../studio/vault/ and contains devlog/drafts/m1.1-spacecraft-control.md, devlog/assets/m1.1-hero-first-burn.png, ten files under devlog/technical/m1.1/, and project/roadmap-detail/M1.1.md</precondition>
  <reversibility rating="one-way">These URLs become public the moment the deploy
  fires; Discord embeds, RSS guids and vault references pin them, so a later
  rename needs a redirect stub rather than an edit.</reversibility>
  <files>devlog/, assets/, technical/m1.1/, roadmap/M1.1.md, pages/roadmap.md</files>
  <read_first>
    - `.planning/phases/04-analytics-launch-content-deploy-hardening/04-RESEARCH.md` — Pattern 5 (the verified source-to-destination promote map), Pitfall 4 (why the whole tree must land in one commit), Pitfall 5 (the launch post's extra frontmatter key is tolerated by the non-strict schema and must not be stripped), Pitfall 6 (the placeholder wording variants, all already handled) and Pitfall 7 (double-bracket tokens inside code blocks, which must not be escaped).
    - `devlog/2026-07-13-making-mercury-precess.md` — the promoted-post shape: frontmatter preserved verbatim from the studio draft, body hero reference pointing one directory up into assets/.
    - `src/content.config.ts` — the four collection loaders and their filename rules; the technical rule accepts decimal phase numbers, the devlog schema is non-strict, and the technical and roadmap schemas are strict.
    - `src/lib/hero-assets.ts` — hero detection runs off entry.assetImports and an eager glob of the assets directory, so a promoted PNG is picked up with no code change; the prose hero_visual frontmatter value is deliberately not used.
    - `CLAUDE.md` — the content section rule: these four trees are drop targets, never edited in place.
  </read_first>
  <action>
    Promote the full D-63 M1.1 tree into this repo in a single commit. Copy every
    file byte-for-byte with no reformatting, re-wrapping, frontmatter edits or
    escaping of any kind.

    Sources and destinations. The announcement
    ../studio/vault/devlog/drafts/m1.1-spacecraft-control.md becomes a file in
    devlog/ named per the slug chosen in Task 1. Its hero
    ../studio/vault/devlog/assets/m1.1-hero-first-burn.png becomes
    assets/m1.1-hero-first-burn.png, matching the M0.7 and M0.8 hero placement so
    the body's relative reference resolves. All ten files under
    ../studio/vault/devlog/technical/m1.1/ become technical/m1.1/ under the same
    filenames, including the three decimal-numbered ones.
    ../studio/vault/project/roadmap-detail/M1.1.md becomes roadmap/M1.1.md. The
    refreshed transcription from Task 3 replaces pages/roadmap.md.

    Excluded, per D-63: the .discord.txt sibling of the announcement, which is
    Discord post copy and never a site page, and how-this-gets-built.md, which the
    studio has marked unpublished pending its VOICE pass. Neither is copied. Do not
    "helpfully" promote either one; the second would additionally crash the build,
    because its status value is outside the devlog schema enum — which is the
    loud-fail path Plan 04-01 Task 3 proves.

    Do not strip the launch post's extra frontmatter key. The devlog schema is
    non-strict, so the unknown key is dropped at parse time and the post loads as
    is; removing it from the promoted file would violate the drop-target rule and
    changing the schema to accept it would be scope the phase does not have.

    Everything downstream is automatic and needs no code change: newest-first date
    ordering puts this post at the top of both the archive and the feed through the
    shared collection query, resolvePhase resolves the roadmap document's deep-dive
    placeholders against the newly present technical/m1.1/ files, milestoneSortKey
    sorts the new milestone after every m0 entry, and the roadmap overview
    generates its detail link from the roadmap collection.

    Land all of it in one commit. The config-load preflight validates every content
    tree together, so a roadmap document promoted without its deep-dives fails the
    build — and D-08 means a half-promoted push is a red deploy.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `diff ../studio/vault/devlog/technical/m1.1/phase-53.1-demo-control-policy-attitude-indicator.md technical/m1.1/phase-53.1-demo-control-policy-attitude-indicator.md` reports no difference, and the same holds for every other promoted markdown file against its source.
    - `ls technical/m1.1/*.md | wc -l` is 10.
    - `test -f assets/m1.1-hero-first-burn.png` succeeds and `cmp` against the studio source reports no difference.
    - `test ! -e devlog/how-this-gets-built.md` and no `.discord.txt` file exists anywhere under `devlog/`.
    - `grep -q 'tags:' devlog/<launch-post>.md` succeeds — the extra frontmatter key survives the promote unmodified.
    - `npm run build` exits 0 and `find dist/devlog -name index.html | wc -l` is 10.
    - `grep -rc technical-devlog dist/ | grep -v ':0$' | wc -l` is 0 — the Discord-era placeholder wording survives nowhere in the built site.
    <!-- planner-discipline-allow: technical-devlog -->
    - The launch post's title appears at a smaller byte offset than the manifesto's in `dist/index.html`, and its guid is the first item in `dist/rss.xml`.
    - `dist/roadmap/m1.1/index.html` exists and contains at least one href into `technical/m1.1/`.
    - `dist/technical/m1.1/phase-53.1-demo-control-policy-attitude-indicator/index.html` exists.
    - The launch post page's `og:image` meta content ends with a hashed build of the promoted hero PNG.
    - `npm test` exits 0 — every assertion Task 2 retargeted now passes.
  </acceptance_criteria>
  <done>The launch post tops the archive and the feed, all ten deep-dives and the roadmap detail page render with resolved cross-links, and the whole suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| studio vault → this repo | Markdown and a binary image cross a repository boundary and are rendered as site content |
| promoted markdown → rendered HTML | Author-controlled content passes through the Markdown pipeline into public pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | promoted markdown fidelity | medium | mitigate | Every promoted file is byte-compared against its studio source in acceptance criteria; a promote that edits content fails the plan |
| T-04-06 | Information disclosure | promoting an unpublished studio document | high | mitigate | The unpublished document is explicitly excluded, and its out-of-enum status value additionally crashes the build — the loud-fail path Plan 04-01 Task 3 proves empirically |
| T-04-07 | Spoofing | markdown injection through promoted content | low | accept | Content is git-trusted and authored by the project owner; the pipeline already escapes attribute context and the feed already runs an allow-list sanitiser |
| T-04-08 | Tampering | binary hero asset crossing repos | low | mitigate | The PNG is byte-compared to its source with cmp; it is served as a static asset and never executed |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs occur in this plan or phase (04-RESEARCH Package Legitimacy Audit: zero new dependencies). package.json must be byte-identical after this plan |
</threat_model>

<verification>
- `npm run build` exits 0 and produces 10 devlog pages, 66 technical pages, 9 roadmap detail pages.
- `npm test` exits 0 across the whole harness.
- Every promoted file byte-matches its studio-vault source.
- `git diff --stat package.json package-lock.json` is empty.
- The studio repository has one new commit and is left unpushed.
</verification>

<success_criteria>
- The launch post is the first archive entry and the first RSS item.
- All ten M1.1 deep-dives, including the decimal-numbered ones, render and are indexed.
- The M1.1 roadmap detail page renders with resolved deep-dive links and is linked from the overview.
- Neither the site's overview nor its M1.1 detail page describes M1.1 as in progress.
- No excluded studio document reached the repo.
</success_criteria>

## Artifacts this phase produces

Symbols and files created by Phase 4 (excluded from drift verification — they do
not exist in the codebase before this phase):

- `devlog/2026-07-30-first-burn.md` (exact slug fixed by Task 1) — promoted launch announcement
- `assets/m1.1-hero-first-burn.png` — promoted hero image
- `technical/m1.1/phase-48…phase-54` (10 files, including phase-53.1, 53.2, 53.3) — promoted deep-dives
- `roadmap/M1.1.md` — promoted milestone detail document
- `../studio/vault/devlog/drafts/roadmap.md` — new studio-side transcription source
- `tests/hardening.smoke.sh`, `tests/live-probe.sh`, `buildSha`, `build-sha`, `PROBE_ATTEMPTS`, `PROBE_DELAY` (Plan 04-01)
- `GOATCOUNTER_CODE`, `GOATCOUNTER_PLACEHOLDER`, `assertGoatcounterConfigured` (Plan 04-03)
- `SLUG_REDIRECTS` (Plan 04-04)
- `smoke` job and `page_url` job output in `.github/workflows/deploy.yml` (Plan 04-05)

## Flagged Assumptions

- **CONT-05 / unclassified edge** — the edge probe could not classify CONT-05
  into a shape category. Read here as: "publish the launch post" is a content
  operation with no input-shape boundary of its own; its edges belong to the
  collection loaders and orderers, all of which are already covered by existing
  assertions plus the retargeted counts in Task 2. If a reviewer identifies a
  distinct edge class for CONT-05, it is unplanned.
- **M1.1 detail document status prose (Task 3, second half)** — D-64 names
  `pages/roadmap.md` explicitly and does not name the M1.1 detail document. This
  plan extends the same refresh to that document's two stale status statements
  because leaving them ships a page that says M1.1 is in progress, which is the
  exact outcome D-64's stated rationale forbids. Scope is deliberately limited to
  those two statements. If the developer prefers the letter of D-64 over its
  rationale, drop the second half of Task 3 and accept the contradiction.
- **Phase 54 has no section in the M1.1 detail document** while a
  `phase-54-findings-and-fixes.md` deep-dive exists. This is not a build problem
  (the document only fails when a placeholder cannot resolve, and it carries no
  Phase 54 placeholder) and authoring a new roadmap section is studio-side content
  work outside this phase. Recorded, not planned.

<output>
Create `.planning/phases/04-analytics-launch-content-deploy-hardening/04-02-SUMMARY.md` when done
</output>
