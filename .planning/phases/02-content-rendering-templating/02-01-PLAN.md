---
phase: 02-content-rendering-templating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - devlog/2026-04-13-before-the-galaxy-a-triangle.md
  - devlog/2026-04-25-a-coordinate-system-that-doesnt-lie.md
  - devlog/2026-06-05-a-moon-that-actually-orbits.md
  - devlog/2026-06-14-from-one-world-to-many.md
  - devlog/2026-06-19-teaching-the-engine-to-fast-forward.md
  - devlog/2026-06-25-shipping-the-warp-lever-with-a-bouncer-on-the-door.md
  - devlog/2026-07-10-warping-without-losing-the-moon.md
  - devlog/2026-07-13-making-mercury-precess.md
  - assets/m0.7-hero-contrast.png
  - assets/m0.8-hero-precession.png
  - technical/
  - roadmap/
  - pages/how-its-made.md
  - pages/roadmap.md
  - src/content.config.ts
  - tests/build.smoke.sh
  - PROMOTE-MANIFEST.md
autonomous: true
requirements: [CONT-02, CONT-03, CONT-04]
must_haves:
  truths:
    - "All 9 announcement posts (manifesto + M0.1–M0.8) exist as .md files in devlog/ with YYYY-MM-DD-slug.md names"
    - "All 55 M0.1–M0.8 technical deep-dives plus _how-to-read.md exist under technical/ with vault milestone subdirs preserved"
    - "All 8 roadmap detail docs exist as roadmap/M0.1.md … roadmap/M0.8.md"
    - "pages/how-its-made.md and pages/roadmap.md exist and are publishable (not status: draft)"
    - "npm run build still exits 0 after every promoted file lands"
  artifacts:
    - devlog/ (9 .md files)
    - technical/m0.1 … technical/m0.8 (55 .md) + technical/_how-to-read.md
    - roadmap/M0.1.md … roadmap/M0.8.md
    - pages/how-its-made.md
    - pages/roadmap.md
    - assets/m0.7-hero-contrast.png
    - assets/m0.8-hero-precession.png
    - PROMOTE-MANIFEST.md
  key_links:
    - "devlog announcement frontmatter `milestone: M0.X` is the join key to technical/m0.x/ and roadmap/M0.X.md (Plan 06/07 consume it)"
    - "Relative image path ../assets/*.png inside two announcements resolves to repo-root assets/ — Astro's collect-images plugin processes it at build"
    - "src/content.config.ts devlog status enum must accept `final` or M0.7/M0.8 fail Zod validation"
---

<objective>
Promote every piece of locked studio content for closed milestones M0.1–M0.8 into this repo's four read-only content trees, so every later plan in this phase builds against real content rather than placeholders (D-25, D-43).

Purpose: this is the Wave 0 gap named in 02-VALIDATION.md — every `❌ W0` verification row in the phase is blocked on the content physically existing here. Nothing downstream can be honestly verified against an empty tree.
Output: `devlog/` grows from 1 to 9 posts; `technical/`, `roadmap/`, `pages/` and `assets/` are created; `PROMOTE-MANIFEST.md` records the exact source→destination mapping so the promote is auditable and repeatable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-rendering-templating/02-CONTEXT.md
@.planning/phases/02-content-rendering-templating/02-RESEARCH.md
@CLAUDE.md
@src/content.config.ts
@tests/build.smoke.sh
</context>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Notes |
|---|---|---|
| `devlog/<8 new files>` | content | Announcement archive, promoted + renamed to `YYYY-MM-DD-slug.md` |
| `assets/m0.7-hero-contrast.png`, `assets/m0.8-hero-precession.png` | asset | Referenced as `../assets/…` from two announcement bodies (D-28) |
| `technical/` (dir, 56 files) | content | New read-only promote drop target (D-31) |
| `roadmap/` (dir, 8 files) | content | New read-only promote drop target (D-38) |
| `pages/how-its-made.md`, `pages/roadmap.md` | content | New read-only promote drop target (D-26); `roadmap.md` is authored, not copied (D-37) |
| `PROMOTE-MANIFEST.md` | doc | Source→destination mapping + exclusion rationale |
| `src/content.config.ts` — devlog `status` enum widened | config | Accepts `final` (M0.7/M0.8 carry it) |

<tasks>

<task type="auto">
  <name>Task 1: Promote the 8 milestone announcements and their two hero assets into devlog/</name>
  <files>devlog/2026-04-13-before-the-galaxy-a-triangle.md, devlog/2026-04-25-a-coordinate-system-that-doesnt-lie.md, devlog/2026-06-05-a-moon-that-actually-orbits.md, devlog/2026-06-14-from-one-world-to-many.md, devlog/2026-06-19-teaching-the-engine-to-fast-forward.md, devlog/2026-06-25-shipping-the-warp-lever-with-a-bouncer-on-the-door.md, devlog/2026-07-10-warping-without-losing-the-moon.md, devlog/2026-07-13-making-mercury-precess.md, assets/m0.7-hero-contrast.png, assets/m0.8-hero-precession.png, src/content.config.ts, tests/build.smoke.sh</files>
  <read_first>
    - `src/content.config.ts` — the devlog collection's `FILENAME_RE`, `generateId` throw, and Zod `status` enum; this task widens exactly one enum and nothing else
    - `tests/build.smoke.sh` — the committed Phase 1 harness this task extends (it is the phase's only test runner; no framework exists or is being added)
    - `devlog/2026-04-07-why-im-building-a-hyperrealistic-space-sim.md` — the already-promoted manifesto, the naming precedent for the 8 new filenames
    - `../studio/vault/devlog/drafts/m0.7-nested-wisdom-holman.md` and `../studio/vault/devlog/drafts/m0.8-perturbations.md` — read the frontmatter (`status: final`) and the body's `![…](../assets/…png)` image line before copying
  </read_first>
  <action>
Extend the RED gate first, then make it green.

RED step — edit `tests/build.smoke.sh`: in the positive-check block, after the existing manifesto assertions, add an assertion that the built archive lists nine announcements. Use a count of the rendered list rows in `dist/index.html` (the Phase 1 throwaway page renders one `<li` per post) and require exactly `9`. Run the harness and confirm it fails at 1 before promoting. Do not weaken the existing manifesto/date/base-path assertions.

GREEN step — copy the 8 milestone announcement drafts from `../studio/vault/devlog/drafts/` into `devlog/`, renaming each to `YYYY-MM-DD-slug.md` where the date is the file's frontmatter `date:` and the slug is the kebab-cased frontmatter `title:` (apostrophes and em dashes dropped, not transliterated). The mapping is fixed — use exactly these eight destination names, because CONT-06 (Phase 4) will freeze them as permanent URLs:
  - `m0.1-vulkan-bootstrap.md` → `2026-04-13-before-the-galaxy-a-triangle.md`
  - `m0.2-coordinate-system.md` → `2026-04-25-a-coordinate-system-that-doesnt-lie.md`
  - `m0.3-basic-orbit.md` → `2026-06-05-a-moon-that-actually-orbits.md`
  - `m0.4-multi-body.md` → `2026-06-14-from-one-world-to-many.md`
  - `m0.5-gravity-performance.md` → `2026-06-19-teaching-the-engine-to-fast-forward.md`
  - `m0.6-wh-shipped.md` → `2026-06-25-shipping-the-warp-lever-with-a-bouncer-on-the-door.md`
  - `m0.7-nested-wisdom-holman.md` → `2026-07-10-warping-without-losing-the-moon.md`
  - `m0.8-perturbations.md` → `2026-07-13-making-mercury-precess.md`

Copy byte-for-byte — no reflow, no frontmatter edits, no body edits (D-14, D-25; VOICE.md is locked studio-side). Explicitly EXCLUDE `how-this-gets-built.md` (frontmatter `status: skeleton`, scheduled after M1.1 close), `how-its-made.md` (that is Task 3's `pages/` source, not an announcement), `manifesto.md` (already promoted in Phase 1), and `m0.8-perturbations.discord.txt` (not a post — D-25). Selection is this explicit named list, never a directory glob.

Copy `../studio/vault/devlog/assets/m0.7-hero-contrast.png` and `m0.8-hero-precession.png` to a new repo-root `assets/` directory (D-28). Repo-root `assets/` is the destination precisely because the two bodies reference `../assets/<file>.png` relative to the `.md`, and keeping that path byte-identical is what D-28 requires; Astro's Sätteri collect-images plugin resolves the relative URL against each entry's `filePath` and emits the optimised asset into `dist/`.

Then widen the devlog Zod `status` field in `src/content.config.ts` from the two-value enum to a three-value one that also accepts `final` — M0.7 and M0.8 carry `status: final` and the current enum would reject them. Keep the render-side rule unchanged: only `draft` hides an entry (D-30), so `final` behaves as published with no filter change anywhere. Add a one-line WHY comment naming that the third value exists because two promoted announcements use it; do not restate the enum values in prose.

Confirm `assets/` is not covered by `.gitignore` and that the two `.png` files are actually staged by git.
  </action>
  <verify>
    <automated>npm run build && bash tests/build.smoke.sh && ls devlog/*.md | wc -l | grep -qx 9 && test -f assets/m0.7-hero-contrast.png && test -f assets/m0.8-hero-precession.png && git status --porcelain assets/ | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `ls devlog/*.md | wc -l` outputs `9`
    - All eight destination filenames above exist verbatim under `devlog/`
    - `diff ../studio/vault/devlog/drafts/m0.8-perturbations.md devlog/2026-07-13-making-mercury-precess.md` reports no differences (byte-identical promote)
    - `ls devlog/ | grep -c 'how-this-gets-built\|discord.txt\|^manifesto'` outputs `0`
    - `npm run build` exits 0 (proves the widened `status` enum accepts `final` for M0.7/M0.8)
    - `bash tests/build.smoke.sh` exits 0, including the new nine-entry assertion and the still-passing D-10 negative check
    - `grep -rn 'assets/' .gitignore` returns nothing, and `git status --porcelain assets/` lists both PNGs
    - `grep -c '<li' dist/index.html` outputs `9`
  </acceptance_criteria>
  <done>Nine announcements and two hero assets are in the repo, the build is green, and the smoke harness proves the archive count.</done>
</task>

<task type="auto">
  <name>Task 2: Promote the 55 technical deep-dives and the 8 roadmap detail docs</name>
  <files>technical/_how-to-read.md, technical/m0.1/, technical/m0.2/, technical/m0.3/, technical/m0.4/, technical/m0.5/, technical/m0.6/, technical/m0.7/, technical/m0.8/, roadmap/M0.1.md, roadmap/M0.2.md, roadmap/M0.3.md, roadmap/M0.4.md, roadmap/M0.5.md, roadmap/M0.6.md, roadmap/M0.7.md, roadmap/M0.8.md</files>
  <read_first>
    - `../studio/vault/devlog/technical/_how-to-read.md` — the tag legend; it renders at `/technical/how-to-read/` (D-32) so its filename's leading underscore must survive the copy
    - `../studio/vault/devlog/technical/m0.3/phase-14.5-swapchain-acquire-fix.md` — the single file in the whole corpus containing a real Obsidian wikilink; confirm it copies unmodified
    - `../studio/vault/project/roadmap-detail/M0.1.md` — the roadmap detail shape (`**Era:**` line, `## Phase N: Title` headings, `**Deep-dive:** posted in #technical-devlog` placeholder)
    - `CLAUDE.md` (repo root) — the read-only drop-target rule that now extends to these two new trees
  </read_first>
  <action>
Create `technical/` at repo root and copy `../studio/vault/devlog/technical/` into it, preserving the milestone subdirectory structure exactly: `_how-to-read.md` at the tree root, and `m0.1/` … `m0.8/` each containing their `phase-NN[.N]-slug.md` files. Expected counts, which must match exactly: m0.1=6, m0.2=6, m0.3=8, m0.4=8, m0.5=8, m0.6=6, m0.7=8, m0.8=5, total 55, plus `_how-to-read.md` = 56 files.

EXCLUDE `m1.1/` entirely — its 5 deep-dives ship in Phase 4 alongside the launch post (D-44). Do not create an empty `m1.1/` directory either; nothing about the templates may assume the corpus is closed, but nothing should ship early.

Create `roadmap/` at repo root and copy `../studio/vault/project/roadmap-detail/M0.1.md` … `M0.8.md` into it, filenames unchanged (D-38 derives the milestone from the capitalised filename). EXCLUDE `M1.1.md` (Phase 4, D-44).

Every copy is byte-for-byte. These trees carry no frontmatter at all (D-33) — do not add any, do not normalise headings, do not rewrite the `Deep-dive:` placeholder lines. That placeholder is resolved at render time by Plan 07, never in the content.

Extend the repo-root `CLAUDE.md` "Content" section so the read-only promote-drop rule names all four trees rather than `devlog/` alone. Keep the wording style of the existing sentence.
  </action>
  <verify>
    <automated>test $(find technical -name '*.md' | wc -l) -eq 56 && test $(find technical/m0.1 -name '*.md' | wc -l) -eq 6 && test $(find technical/m0.8 -name '*.md' | wc -l) -eq 5 && test $(ls roadmap/*.md | wc -l) -eq 8 && test ! -d technical/m1.1 && test ! -f roadmap/M1.1.md && diff -r ../studio/vault/project/roadmap-detail/M0.3.md roadmap/M0.3.md && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `find technical -name '*.md' | wc -l` outputs `56`
    - `test -f technical/_how-to-read.md` succeeds (leading underscore preserved)
    - Per-milestone counts are exactly 6/6/8/8/8/6/8/5 for m0.1…m0.8
    - `test ! -d technical/m1.1` and `test ! -f roadmap/M1.1.md` both succeed
    - `ls roadmap/*.md | wc -l` outputs `8`
    - `diff -r ../studio/vault/devlog/technical/m0.3 technical/m0.3` reports no differences
    - `grep -c 'phase-03-rendering-pipeline' technical/m0.3/phase-14.5-swapchain-acquire-fix.md` outputs `1` (the one real wikilink survived the copy intact)
    - `grep -c 'technical/' CLAUDE.md` is at least `1`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Both new trees are in the repo at the exact expected file counts, byte-identical to the vault, with M1.1 content correctly withheld.</done>
</task>

<task type="auto">
  <name>Task 3: Land pages/how-its-made.md and author the site-voice roadmap overview</name>
  <files>pages/how-its-made.md, pages/roadmap.md, PROMOTE-MANIFEST.md</files>
  <read_first>
    - `../studio/vault/devlog/drafts/how-its-made.md` — note its frontmatter carries `title`, `status`, `audience`, `updated` and that its status value currently marks it as unpublished staging
    - `../studio/vault/devlog/discord/roadmap-overview.pinned.md` — the reference text for the overview; read it in full, it is short
    - `.planning/phases/02-content-rendering-templating/02-CONTEXT.md` — D-37's explicit instruction that the pin is transcribed, never rendered verbatim
    - `../studio/vault/devlog/VOICE.md` — the register the authored overview must match
  </read_first>
  <action>
Create `pages/` at repo root as the third new read-only drop target (D-26).

Copy `how-its-made.md` into `pages/how-its-made.md`, body byte-identical. One frontmatter field changes: its publication status flips from the studio-side staging value to `published`. Rationale to record in `PROMOTE-MANIFEST.md`: D-30 makes a staged entry non-rendering, and CONT-03 requires this page live at M1.1 close — the act of promoting IS the publication, and the file's own frontmatter comment already says it publishes to this site at M1.1 close. This single-field publication-state normalisation is the only content mutation permitted anywhere in this plan; the `updated:` date, title, and every body line stay exactly as-is.

Author `pages/roadmap.md` — this file is written, not copied (D-37). Frontmatter: `title` naming the roadmap, `updated: 2026-07-22`, `status: published`. Body starts with an `# H1` title (the layout takes its title from the body H1, D-14/D-20). Transcribe `roadmap-overview.pinned.md` into site voice:
  - DROP the "How this channel works" paragraph entirely — it describes Discord mechanics for content this site now hosts itself.
  - Rewrite the "confused by tags" pointer so it refers to the How to Read document by name as part of this site's technical section, not as a pinned Discord post.
  - KEEP the era arc line (Era 0 → Era 1 → Era 2 → Era 3+) and the Era 0 milestone list with its per-milestone one-line summaries and phase counts, in the same order.
  - KEEP M1.1 listed as in progress under Era 1, with no detail-page reference — its detail doc lands in Phase 4 (D-44).
  - DROP the closing "follow the milestone threads below" line; the per-milestone links are generated site chrome added by Plan 07, not prose.
  - Write NO absolute URLs and NO `/interstellar-website` path prefixes anywhere in the body. SITE-02 keeps the base path in one config location; navigation into the detail pages is generated chrome, so the prose only needs to name things, not link them.

Create `PROMOTE-MANIFEST.md` at repo root: a table of every source path under `../studio/vault/` mapped to its repo destination for all four trees (referencing Tasks 1 and 2's mappings), a short "Excluded and why" list covering `how-this-gets-built.md`, `m0.8-perturbations.discord.txt`, `technical/m1.1/`, and `roadmap-detail/M1.1.md`, and a note that `pages/roadmap.md` is authored rather than copied. State plainly that this repo hosts no promote automation (that is a studio-side concern per REQUIREMENTS.md "Out of Scope") and that this manifest is the audit record for the manual promote.
  </action>
  <verify>
    <automated>test -f pages/how-its-made.md && test -f pages/roadmap.md && test -f PROMOTE-MANIFEST.md && grep -qx 'status: published' pages/how-its-made.md && grep -q '^updated:' pages/roadmap.md && test $(grep -c 'interstellar-website' pages/roadmap.md) -eq 0 && test $(grep -c 'How this channel works' pages/roadmap.md) -eq 0 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `pages/how-its-made.md`, `pages/roadmap.md`, `PROMOTE-MANIFEST.md` all exist
    - `grep -qx 'status: published' pages/how-its-made.md` succeeds
    - `diff <(tail -n +8 ../studio/vault/devlog/drafts/how-its-made.md) <(tail -n +8 pages/how-its-made.md)` reports no body differences
    - `grep -c 'interstellar-website' pages/roadmap.md` outputs `0` <!-- planner-discipline-allow: interstellar-website -->
    - `grep -c 'How this channel works' pages/roadmap.md` outputs `0` <!-- planner-discipline-allow: How this channel works -->
    - `grep -c '^# ' pages/roadmap.md` outputs `1` and `grep -c 'M1.1' pages/roadmap.md` is at least `1`
    - `grep -c 'M0.1\|M0.2\|M0.3\|M0.4\|M0.5\|M0.6\|M0.7\|M0.8' pages/roadmap.md` is at least `8`
    - `PROMOTE-MANIFEST.md` names all four excluded sources
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All four content trees are populated, the promote is audited in a manifest, and the roadmap overview reads as site prose rather than a Discord pin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `../studio/vault/` → this repo | Cross-repo file copy; both repos are controlled by the same operator and are git-tracked, so content is trusted, but selection errors cross here |
| repo → published static site | Anything landed in the four trees becomes public HTML on the next deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Information Disclosure | Promote selection from `../studio/vault/devlog/drafts/` | medium | mitigate | Selection is an explicit named eight-file list, never a directory glob; `how-this-gets-built.md` (unreleased, post-M1.1) and `m0.8-perturbations.discord.txt` are named exclusions with an acceptance criterion asserting their absence (Task 1) |
| T-02-05 | Information Disclosure | Premature M1.1 content publication | medium | mitigate | `technical/m1.1/` and `roadmap-detail/M1.1.md` are excluded with `test ! -d` / `test ! -f` acceptance criteria (Task 2); D-44 defers them to Phase 4 |
| T-02-06 | Tampering | Byte-fidelity of promoted VOICE-locked prose | high | mitigate | Every copy verified by `diff`/`diff -r` against its vault source; exactly one field-level mutation is permitted and it is recorded in `PROMOTE-MANIFEST.md` (Task 3) |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — nothing to audit here; `@astrojs/sitemap` is audited in Plan 03 |
</threat_model>

<verification>
- `npm run build` exits 0 with all four trees present
- `bash tests/build.smoke.sh` exits 0 including the new nine-announcement assertion
- File counts: devlog 9, technical 56, roadmap 8, pages 2, assets 2
- No M1.1 content anywhere in the repo
- `git status --porcelain` shows only intended additions
</verification>

<success_criteria>
Every piece of locked studio content for M0.1–M0.8 is in this repo, byte-identical to its vault source except for one documented publication-state flip, with an auditable manifest and a green build.
</success_criteria>

<output>
Create `.planning/phases/02-content-rendering-templating/02-01-SUMMARY.md` when done. Record the exact eight announcement filenames chosen — Phase 4 (CONT-06) freezes them as permanent URLs.
</output>
