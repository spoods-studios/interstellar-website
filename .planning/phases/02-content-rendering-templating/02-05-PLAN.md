---
phase: 02-content-rendering-templating
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - src/pages/index.astro
  - tests/build.smoke.sh
autonomous: false
requirements: [CONT-02, CONT-03]
must_haves:
  truths:
    - "The site root lists all 9 announcements newest-first, each linking to its post"
    - "Each archive row shows the post title, its date, and its milestone tag where one exists"
    - "One developer-approved sentence describing the project sits above the list"
    - "Neither standalone page appears anywhere in the archive listing"
  artifacts:
    - src/pages/index.astro
  key_links:
    - "The archive queries the devlog collection only — the structural separation of the pages collection is what keeps standalone pages out of the archive and, in Phase 3, out of RSS"
    - "Archive ordering must match the prev/next ordering in src/pages/devlog/[slug].astro"
---

<objective>
Replace Phase 1's throwaway ingestion-proof page with the real archive homepage: the site root becomes the announcement archive (D-13), carrying one developer-approved sentence of top matter (D-16).

Purpose: this closes the announcement half of CONT-02 and demonstrates CONT-03's structural exclusion — the standalone pages live in a different collection, so no filtering logic is needed to keep them out of the archive.
Output: a rewritten `src/pages/index.astro`.

**This plan is not autonomous.** D-16 requires explicit developer approval of the homepage sentence's exact wording, and the UI-SPEC currently carries only a placeholder. Task 1 is a blocking decision checkpoint; the placeholder must not ship.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-rendering-templating/02-UI-SPEC.md
@.planning/phases/02-content-rendering-templating/02-CONTEXT.md
@src/pages/index.astro
@src/layouts/BaseLayout.astro
@src/lib/content-guards.ts
@src/lib/title-from-h1.ts
</context>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Notes |
|---|---|---|
| `src/pages/index.astro` | route | Replaces the Phase 1 throwaway page wholesale; emits the archive at the site root |
| Approved homepage sentence | copy | Recorded verbatim in the plan summary and in `02-UI-SPEC.md`'s Copywriting Contract |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Developer sign-off on the homepage one-liner (D-16)</name>
  <files>(none — decision only; the approved sentence lands in src/pages/index.astro during Task 2)</files>
  <decision>The exact wording of the single quiet sentence that sits above the archive list on the site root.</decision>
  <context>
D-16 locks the shape — ONE sentence, VOICE register, no hype — but explicitly reserves the exact wording for developer approval at plan review. 02-UI-SPEC.md carries a placeholder marked "must not ship without confirmation". This is the first thing a newcomer reads on the site, so it is not a discretionary default.

Constraints on any answer: one sentence, no marketing superlatives, no exclamation, no "revolutionary"/"next-generation" register, and no claim the engine does not yet deliver. The site's whole posture is "quiet reading surface" — the sentence is context, not a hero line.
  </context>
  <options>
    <option id="option-a">
      <name>"A from-scratch physics-accurate space engine, built in the open."</name>
      <pros>Shortest; the UI-SPEC's own placeholder, so it is already the known baseline; "built in the open" earns the devblog archive directly beneath it.</pros>
      <cons>"physics-accurate" is a claim the reader cannot yet check from the homepage; slightly closer to a tagline than to prose.</cons>
    </option>
    <option id="option-b">
      <name>"A space simulation engine written from scratch, and a written record of how it is being built."</name>
      <pros>Describes exactly what the page below it is; makes no accuracy claim; reads as a sentence rather than a tagline.</pros>
      <cons>Longer; leads with the engine and buries the physics ambition that motivates the whole project.</cons>
    </option>
    <option id="option-c">
      <name>"I am building a space engine on real n-body physics, from scratch, and writing down how it goes."</name>
      <pros>First person, closest to the manifesto's own voice; concrete ("n-body") instead of adjectival; the honest framing D-G's transparency posture implies.</pros>
      <cons>First person on a project homepage is a register choice the developer may not want site-wide; "how it goes" is deliberately informal.</cons>
    </option>
  </options>
  <action>Present the three candidate sentences and their trade-offs to the developer and block until one is chosen or replacement wording is supplied. Record the chosen sentence verbatim in the plan summary; Plan 08 Task 3 writes it into 02-UI-SPEC.md's Copywriting Contract row. Do not proceed to Task 2 with the placeholder wording.</action>
  <verify>
    <automated>test -n "${GSD_D16_APPROVED_SENTENCE:-}" || grep -rq 'D-16' .planning/phases/02-content-rendering-templating/02-05-SUMMARY.md 2>/dev/null || echo "blocked: awaiting developer sign-off"</automated>
  </verify>
  <acceptance_criteria>
    - A single sentence is chosen and recorded verbatim, attributed to the developer
    - The recorded sentence is not the 02-UI-SPEC.md placeholder
  </acceptance_criteria>
  <done>The homepage sentence is approved by the developer and recorded verbatim for Task 2 to use.</done>
  <resume-signal>Select: option-a, option-b, option-c, or supply exact replacement wording. The approved sentence must be recorded verbatim in the plan summary and written into 02-UI-SPEC.md's Copywriting Contract row before the phase closes.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Rewrite the site root as the announcement archive</name>
  <files>src/pages/index.astro, tests/build.smoke.sh</files>
  <read_first>
    - `src/pages/index.astro` — the current throwaway page in full; its query skeleton (collection fetch, non-empty assert, draft filter, date fallback, date-descending sort) carries forward, its render body does not
    - `src/pages/devlog/[slug].astro` — the ordering and title/date derivation this page must match exactly, so the archive and the prev/next chain cannot disagree
    - `src/layouts/BaseLayout.astro` — the shell this page renders through
    - `.planning/phases/02-content-rendering-templating/02-UI-SPEC.md` — "Index/list contract" for the homepage row shape and the Typography section's statement that the one-liner is body-sized context, not a hero
  </read_first>
  <action>
Replace `src/pages/index.astro` wholesale. Render through `BaseLayout` (not `PostLayout` — this is an index, and UI-SPEC gives each index its own template).

Page body, in order:
  1. The developer-approved sentence from Task 1, verbatim, at body size as context above the list (D-16). Do not ship the UI-SPEC placeholder.
  2. A flat, reverse-chronological list of every visible `devlog` entry (D-12 as amended: the flat list applies to the announcement archive only). One row per post: the title as an accent-coloured link to `/devlog/<id>/`, and a meta line carrying the milestone tag and date (D-11). The manifesto has no milestone and renders its date alone.

Query only the `devlog` collection. That single-collection query is the whole of CONT-03's structural exclusion — the standalone pages live in a separate collection (D-26/D-31), so no filter keeps them out and none should be written. In Phase 3 the RSS feed reuses this same query for the same reason.

Reuse the shared helpers rather than re-inlining Phase 1's logic: `assertNonEmpty` for the loader-base guard, `isVisible` for the draft filter, `titleFromH1` for the manifesto's title fallback. Derive the date from frontmatter when present and from the filename otherwise, and sort descending — identical to the post route's ordering.

Use `set:html` for rendered titles (apostrophes appear in several real titles). Compose every href from `import.meta.env.BASE_URL`.

Update `tests/build.smoke.sh`: the nine-entry assertion added in Plan 01 counted list rows on the throwaway page — re-point it at whatever element the new archive uses for a row, keeping the required count at nine. Add assertions that the newest post appears before the oldest in the emitted HTML, that the approved sentence is present verbatim, and that neither standalone page's slug appears in the archive output.
  </action>
  <verify>
    <automated>npm run build && bash tests/build.smoke.sh && node tests/lib.smoke.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `dist/index.html` contains nine links whose href matches the devlog post path shape
    - `dist/index.html` contains the approved sentence verbatim and does not contain the UI-SPEC placeholder sentence
    - `grep -c 'how-its-made' dist/index.html` outputs `0` <!-- planner-discipline-allow: how-its-made -->
    - `grep -c 'M0.8' dist/index.html` is at least `1` and `grep -c 'M0.1' dist/index.html` is at least `1`
    - The byte offset of the newest post's title in `dist/index.html` is smaller than that of the manifesto's title (newest first)
    - `grep -c '<script' dist/index.html` outputs `0`
    - `src/pages/index.astro` imports `assertNonEmpty`, `isVisible` and `titleFromH1` rather than re-declaring that logic inline
    - `! grep -rIn -e 'github\.io' -e '/interstellar-website' src/` still succeeds
    - `bash tests/build.smoke.sh` exits 0
  </acceptance_criteria>
  <done>The site root is the announcement archive, carrying approved top matter and nine correctly ordered rows, with the standalone pages structurally absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collection query → public index | Whatever this query returns becomes the site's front door, and in Phase 3 its RSS feed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Information Disclosure | Archive index query | medium | mitigate | Query is scoped to the `devlog` collection and filtered through the shared `isVisible` guard; the standalone pages are structurally in a different collection rather than filtered out by string matching (D-26/D-31) |
| T-02-11 | Repudiation | Unapproved homepage copy shipping silently | medium | mitigate | D-16 wording is a blocking decision checkpoint (Task 1); an acceptance criterion asserts the placeholder sentence is absent from the built output |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan |
</threat_model>

<verification>
- `npm run build` exits 0
- `bash tests/build.smoke.sh` exits 0 with the re-pointed nine-row assertion
- The approved sentence, not the placeholder, appears in `dist/index.html`
- Neither standalone page appears in the archive output
</verification>

<success_criteria>
A visitor landing on the site root sees one approved sentence of context and a correctly ordered list of all nine announcements, each of which opens and reads correctly.
</success_criteria>

<output>
Create `.planning/phases/02-content-rendering-templating/02-05-SUMMARY.md` when done. Record the approved D-16 sentence verbatim and note that `02-UI-SPEC.md`'s Copywriting Contract row was updated to match.
</output>
