---
phase: 02-content-rendering-templating
plan: 03
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/phase-sort.ts
  - src/lib/milestone-key.ts
  - src/lib/title-from-h1.ts
  - src/lib/content-guards.ts
  - src/lib/toc.ts
  - src/lib/wikilink-resolver.mjs
  - src/lib/mdast-wikilinks.mjs
  - src/content.config.ts
  - astro.config.mjs
  - package.json
  - package-lock.json
  - tests/lib.smoke.mjs
  - tests/run-all.sh
  - tests/collections.smoke.sh
  - tests/markdown.smoke.sh
autonomous: true
requirements: [CONT-02, CONT-03, CONT-04, SITE-04]
must_haves:
  truths:
    - "All four content trees are queryable as Astro collections with per-tree filename rules"
    - "A non-conforming filename in technical/ or roadmap/ fails the build naming the offending file"
    - "A misconfigured loader base fails the build instead of silently yielding an empty collection"
    - "Obsidian wikilinks in the corpus resolve to real internal site URLs at build time; an unresolvable target fails the build naming file and link"
    - "The C++ [[nodiscard]] attribute inside code is never treated as a wikilink"
    - "C++ code fences render syntax-highlighted in a light theme with zero client JS"
    - "sitemap.xml is emitted into the build output"
  artifacts:
    - src/lib/phase-sort.ts
    - src/lib/milestone-key.ts
    - src/lib/title-from-h1.ts
    - src/lib/content-guards.ts
    - src/lib/toc.ts
    - src/lib/wikilink-resolver.mjs
    - src/lib/mdast-wikilinks.mjs
    - tests/lib.smoke.mjs
    - dist/sitemap-index.xml
  key_links:
    - "astro.config.mjs is the only place the base path is known — the wikilink resolver receives it as an argument, never reads a literal"
    - "satteri({ mdastPlugins }) is additive: Astro's own heading-id, image and Shiki plugins survive, so TOC anchors keep matching"
    - "milestone-key normalisation is the join key for every cross-collection query in Plans 06 and 07"
---

<objective>
Turn the four promoted content trees into four working Astro collections, and land the build-time Markdown machinery this phase depends on: the decimal-aware phase sort, the milestone join key, the H1 title fallback, the TOC derivation, the Sätteri wikilink mdast plugin, light-theme Shiki, and sitemap generation.

Purpose: this is the ingestion and rendering-pipeline layer. Every page template in Plans 04–07 queries these collections and relies on these helpers; getting the loud-fail behaviour and the join keys right here is what stops silent content rot later.
Output: five typed helpers, two `.mjs` build-pipeline modules, three new collections, an extended `astro.config.mjs`, and a committed `tests/lib.smoke.mjs` assertion script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-rendering-templating/02-CONTEXT.md
@.planning/phases/02-content-rendering-templating/02-RESEARCH.md
@.planning/phases/02-content-rendering-templating/02-PATTERNS.md
@.planning/phases/02-content-rendering-templating/02-VALIDATION.md
@src/content.config.ts
@astro.config.mjs
@src/pages/index.astro
@tests/build.smoke.sh
</context>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Signature or shape |
|---|---|---|
| `parsePhaseNumber(idOrFilename: string): number` | exported fn (`src/lib/phase-sort.ts`) | Throws naming the input when no `phase-NN[.N]` segment is present |
| `sortByPhaseNumber<T extends { id: string }>(entries: T[]): T[]` | exported fn (`src/lib/phase-sort.ts`) | Non-mutating; numeric, decimal-aware |
| `normalizeMilestone(raw: string): string` | exported fn (`src/lib/milestone-key.ts`) | `M0.1` / `m0.1` / `M0.1.md` → `m0.1` |
| `milestoneSortKey(raw: string): number` | exported fn (`src/lib/milestone-key.ts`) | Numeric order for milestone lists |
| `titleFromH1(body: string \| undefined, fallback: string): string` | exported fn (`src/lib/title-from-h1.ts`) | First `# ` heading, else fallback |
| `assertNonEmpty<T>(entries: T[], treeName: string): T[]` | exported fn (`src/lib/content-guards.ts`) | Throws naming the tree and the loader base |
| `isVisible(entry): boolean` | exported fn (`src/lib/content-guards.ts`) | D-30 draft filter, one implementation used everywhere |
| `buildToc(headings: MarkdownHeading[]): MarkdownHeading[] \| null` | exported fn (`src/lib/toc.ts`) | H2+H3 only; `null` below the 3×H2 threshold |
| `createWikilinkResolver({ base, technicalRoot })` | exported factory (`src/lib/wikilink-resolver.mjs`) | Returns `resolve(target, fromEntryPath) => string \| null` |
| `createWikilinkPlugin({ resolve })` | exported factory (`src/lib/mdast-wikilinks.mjs`) | Returns a Sätteri `MdastPluginDefinition` with a `text` visitor |
| collections `technical`, `roadmap`, `pages` | Astro collections | Added to `export const collections` beside existing `devlog` |
| `tests/lib.smoke.mjs` | assertion script | Run with `node tests/lib.smoke.mjs`; exits non-zero on failure |
| `tests/run-all.sh` | harness runner | Runs `tests/lib.smoke.mjs` then every `tests/*.smoke.sh` in sorted order; wired to `npm test` |
| `tests/collections.smoke.sh` | harness | Per-collection count assertions + the malformed-filename negative case |
| `tests/markdown.smoke.sh` | harness | Wikilink, code-attribute, Shiki and sitemap build-output assertions |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure helpers — phase sort, milestone key, H1 title, guards, TOC</name>
  <files>src/lib/phase-sort.ts, src/lib/milestone-key.ts, src/lib/title-from-h1.ts, src/lib/content-guards.ts, src/lib/toc.ts, tests/lib.smoke.mjs, tests/run-all.sh, package.json</files>
  <read_first>
    - `src/pages/index.astro` — lines 5-6, 15-19, 21-35: the inline H1 regex, the non-empty assertion, and the draft filter being extracted here
    - `.planning/phases/02-content-rendering-templating/02-RESEARCH.md` — Pattern 1 (`parsePhaseNumber`/`sortByPhaseNumber`), Pattern 3 (`normalizeMilestone`), Pattern 6 (`buildToc`) and assumption A4 on decimal sorting
    - `.planning/phases/02-content-rendering-templating/02-UI-SPEC.md` — "TOC placement and depth": H2+H3 only, 3+ H2 threshold
    - `tests/build.smoke.sh` — the committed-script test precedent this task mirrors in JS
    - `tsconfig.json` — confirm the compiler settings the new `.ts` helpers must satisfy
  </read_first>
  <behavior>
    - `parsePhaseNumber('m0.3/phase-14.5-swapchain-acquire-fix')` → `14.5`
    - `parsePhaseNumber('m0.1/phase-01-window-surface')` → `1`
    - `parsePhaseNumber('how-to-read')` → throws, and the message contains the input string
    - `sortByPhaseNumber` orders ids for phases 14, 14.5, 15, 15.5, 16, 17 in that exact sequence (decimal-aware, not lexical: a lexical sort would put `phase-15` before `phase-14.5`)
    - `sortByPhaseNumber` does not mutate its input array
    - `normalizeMilestone('M0.1')`, `normalizeMilestone('m0.1')`, `normalizeMilestone('M0.1.md')` all → `'m0.1'`
    - `milestoneSortKey('M0.10')` &gt; `milestoneSortKey('M0.9')` (numeric, not string, comparison)
    - `titleFromH1('# A Moon That Actually Orbits\n\nbody', 'fallback')` → `'A Moon That Actually Orbits'`
    - `titleFromH1('no heading here', 'fallback')` → `'fallback'`
    - `titleFromH1` ignores a `#` that is not at the start of a line
    - `assertNonEmpty([], 'technical')` → throws with a message containing `technical`
    - `assertNonEmpty([{}], 'technical')` → returns the array unchanged
    - `isVisible({ data: { status: 'draft' } })` → `false`; `published`, `final`, and absent status → `true`
    - `buildToc` with 3 H2s and 2 H3s → 5 entries, H1 and H4 excluded
    - `buildToc` with 2 H2s → `null`
    - `buildToc` preserves source order
  </behavior>
  <action>
Write `tests/lib.smoke.mjs` FIRST, encoding every case in the behaviour block above using `node:assert/strict`. Structure it as plain top-level assertions with a short label printed per group and a final `ALL CHECKS PASSED` line, mirroring `tests/build.smoke.sh`. No test framework is installed or added — this is a committed script, run as `node tests/lib.smoke.mjs`, exactly the precedent Phase 1 set with the shell harness. Import the helpers by their real `.ts` paths; Node 24 strips types natively, so keep every helper to erasable syntax only (type annotations, `interface`, `type` — no `enum`, no `namespace`, no parameter properties). Run the script and confirm it fails on missing modules before writing any helper.

Then write the five helpers to turn it green:
  - `src/lib/phase-sort.ts` — `parsePhaseNumber` matches a `phase-` prefix followed by digits with an optional single decimal group and returns the parsed float; on no match it throws naming the offending input (D-34, D-10's loud-fail convention). `sortByPhaseNumber` copies the array before sorting and compares numerically.
  - `src/lib/milestone-key.ts` — `normalizeMilestone` lowercases, strips a trailing `.md`, and returns the `m<major>.<minor>` form; `milestoneSortKey` parses the numeric part so milestone lists order correctly past nine (D-35's join key, used by every cross-collection query in Plans 06 and 07).
  - `src/lib/title-from-h1.ts` — `titleFromH1` extracts the first line-anchored `# ` heading from a body string, trims it, and returns the supplied fallback when absent (D-33 for the technical/roadmap trees, D-01 for the frontmatter-less manifesto).
  - `src/lib/content-guards.ts` — `assertNonEmpty` throws a message naming the tree and pointing at the loader `base` in `src/content.config.ts` (RESEARCH Pitfall 1: the glob loader only warns on an empty result, so this assertion is the actual guard); `isVisible` implements D-30 as the single draft-filter implementation the whole site shares, including generated cross-link lists.
  - `src/lib/toc.ts` — `buildToc` keeps depth 2 and 3 only, returns `null` when fewer than three depth-2 headings are present, and otherwise returns the filtered array in source order (D-42, UI-SPEC thresholds).

Create `tests/run-all.sh`: `set -euo pipefail`, `cd` to the repo root, run `node tests/lib.smoke.mjs`, then iterate every `tests/*.smoke.sh` in sorted order and run each with bash, failing on the first non-zero exit. Wire `npm test` to it. Later plans in this phase each add their own `tests/<area>.smoke.sh` file, so this runner is the only place that ever needs to know they exist — nobody has to edit a shared harness file and collide with a parallel plan.
  </action>
  <verify>
    <automated>node tests/lib.smoke.mjs && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/lib.smoke.mjs` exits 0 and prints `ALL CHECKS PASSED`
    - Every behaviour case above has a corresponding assertion in `tests/lib.smoke.mjs`
    - `grep -c 'enum \|namespace ' src/lib/*.ts` outputs `0` (erasable-syntax-only, so Node's type stripping works)
    - `npm test` runs `tests/run-all.sh`, which runs the JS assertions plus every `tests/*.smoke.sh`, and exits 0
    - `grep -rc 'vitest\|jest\|mocha' package.json` outputs `0` (no test framework added)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Five pure helpers exist with a committed assertion script proving the decimal sort, the milestone join key, the title fallback, the guards, and the TOC threshold.</done>
</task>

<task type="auto">
  <name>Task 2: Three new collections with per-tree filename rules and loud failure</name>
  <files>src/content.config.ts, tests/collections.smoke.sh</files>
  <read_first>
    - `src/content.config.ts` — the full current file: the `devlog` glob loader, its project-root-relative `base` comment, its `generateId` throw, and the Zod schema shape the new collections mirror
    - `.planning/phases/02-content-rendering-templating/02-RESEARCH.md` — Pattern 1 (technical `generateId` + regex) and Pattern 2 (roadmap `generateId` + regex), both verified against the installed glob loader source
    - `.planning/phases/02-content-rendering-templating/02-PATTERNS.md` — "src/content.config.ts — extend in place", which states the four-part shape to repeat
    - `technical/` and `roadmap/` — list both trees to confirm the real filenames the regexes must accept, including the decimal-numbered phases
    - `pages/how-its-made.md` and `pages/roadmap.md` — the two `pages` entries and the frontmatter fields they actually carry
  </read_first>
  <action>
Extend `src/content.config.ts` with three collections beside the existing `devlog`, each following the same four-part shape already in the file: a `glob()` loader with a project-root-relative `base`, a `generateId` that throws a message naming the offending file on any non-conforming name, a Zod schema, and inclusion in the final `export const collections`.

  - `technical` — `base: './technical'`, pattern matching Markdown at any depth. `generateId` special-cases the how-to-read file (leading underscore) to the id `how-to-read`, giving it the URL from D-32; every other entry must match a milestone directory followed by `phase-`, a phase number with an optional single decimal, and a slug, and the returned id preserves that `<milestone>/phase-<number>-<slug>` shape so the URL derives from the id with no mapping table. A non-matching filename throws naming the file and stating the required shape (D-33 — this tree has no frontmatter to fall back on).
  - `roadmap` — `base: './roadmap'`, pattern matching the capitalised milestone files. `generateId` matches `M<major>.<minor>.md` case-insensitively and returns the lowercase `m<major>.<minor>` id, so it joins directly against the technical tree's directory names (D-38, D-35 case note). Non-matching filename throws naming the file.
  - `pages` — `base: './pages'`, flat Markdown pattern, default id derivation from the filename. Schema mirrors `devlog`'s permissive all-optional shape with `title`, `status`, `audience`, and `updated` (the meta line for standalone pages reads from `updated`, D-20).

For `technical` and `roadmap`, the schema declares no frontmatter fields at all — these trees carry none (D-33), so the schema's only job is to reject an unexpected key loudly rather than validate one.

Keep the existing project-root-relative `base` comment's warning applicable to all four loaders; add nothing that restates it three more times.

Create a NEW harness file `tests/collections.smoke.sh` (do not edit `tests/build.smoke.sh`; each plan in this phase owns its own harness file). It asserts that each of the four collections resolved to its expected entry count — devlog 9, technical 56, roadmap 8, pages 2 — by counting the corresponding built route directories under `dist/`, or by a small build-time check where a route does not exist yet. RESEARCH Pitfall 1 is the reason this matters: a typo in any loader `base` produces an empty collection and a console warning, not a build failure, so the count is the only thing that catches it. Add a negative case mirroring the existing D-10 block in `tests/build.smoke.sh`: stage a deliberately misnamed file inside `technical/`, confirm the build exits non-zero with the offending filename in the output, then remove it and confirm `technical/` is left clean. Use the same `trap`-and-restore discipline the existing D-10 block uses, because `technical/` is a read-only promote target that must never be left dirty.
  </action>
  <verify>
    <automated>npm run build && bash tests/collections.smoke.sh && bash tests/build.smoke.sh && node tests/lib.smoke.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `src/content.config.ts` exports a `collections` object containing exactly `devlog`, `technical`, `roadmap`, `pages`
    - `npm run build` exits 0 with all four collections resolving
    - `bash tests/collections.smoke.sh` exits 0, including the four count assertions (9 / 56 / 8 / 2)
    - The new negative block fails the build on a misnamed `technical/` file, the error output contains that filename, and `git status --porcelain technical/` is empty afterwards
    - `grep -c "base: './technical'" src/content.config.ts` outputs `1`; same for `'./roadmap'` and `'./pages'`
    - `grep -c "'\.\./technical'" src/content.config.ts` outputs `0` (the Phase 1 base-path landmine is not repeated)
    - The technical `generateId` accepts `m0.3/phase-14.5-swapchain-acquire-fix.md` and `m0.8/phase-46.1-all-planet-validation-seeds.md` (proven by the build succeeding over the real corpus)
  </acceptance_criteria>
  <done>Four collections resolve at their exact expected counts, and a malformed filename in the new trees fails the build by name.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sätteri wikilink mdast plugin, light-theme Shiki, and sitemap</name>
  <files>src/lib/wikilink-resolver.mjs, src/lib/mdast-wikilinks.mjs, astro.config.mjs, package.json, package-lock.json, tests/lib.smoke.mjs, tests/markdown.smoke.sh</files>
  <read_first>
    - `.planning/phases/02-content-rendering-templating/02-RESEARCH.md` — read the POST-RESEARCH CORRECTION banner at the very top BEFORE anything else, then Pattern 4 (the text-node visitor logic, which ports) and Pitfall 2 (the C++ attribute collision)
    - `.planning/phases/02-content-rendering-templating/02-PATTERNS.md` — the correction block and the `astro.config.mjs` additive-keys section
    - `node_modules/satteri/dist/plugin.d.ts` and `node_modules/satteri/dist/mdast/mdast-visitor.d.ts` — the real plugin API: a plugin is an object of per-node-type visitor functions plus a `name`, and structural edits go through the visitor context's insert/remove methods
    - `node_modules/@astrojs/markdown-satteri/dist/processor.d.ts` — the `satteri(opts)` factory signature and its `mdastPlugins` option
    - `technical/m0.3/phase-14.5-swapchain-acquire-fix.md` — the single real wikilink in the corpus, the one live case the resolver must handle
    - `astro.config.mjs` — the current two-key config being extended
  </read_first>
  <behavior>
    - `resolve('../m0.1/phase-03-rendering-pipeline', 'm0.3/phase-14.5-swapchain-acquire-fix')` → the site URL for that deep-dive, base-prefixed, with a trailing slash
    - `resolve('../_how-to-read', 'm0.3/phase-14.5-swapchain-acquire-fix')` → the how-to-read URL from D-32
    - `resolve('m0.1/phase-01-window-surface', 'm0.2/phase-07-anything')` → that deep-dive's URL (non-relative target form also resolves)
    - `resolve('nodiscard', 'm0.2/phase-07-anything')` → `null`
    - `resolve('../m9.9/phase-99-does-not-exist', 'm0.1/phase-01-window-surface')` → `null`
    - A resolver `null` result makes the plugin throw, and the thrown message contains both the source file path and the raw target text
    - The plugin's visitor is registered on text nodes only, so a code node containing the C++ attribute is never offered to it
    - A text node with no bracket pair is left structurally untouched (no spurious node replacement)
    - A labelled wikilink produces a link node whose visible text is the label, not the target
  </behavior>
  <action>
Read the RESEARCH banner first. D-39 is Sätteri, NOT remark. Do not install `@astrojs/markdown-remark`, do not import `unified`, and do not name any file with a `remark-` prefix — RESEARCH.md's Pattern 4/5 registration code is superseded; only its mdast traversal reasoning survives. <!-- planner-discipline-allow: @astrojs/markdown-remark --> <!-- planner-discipline-allow: remark- -->

Install `@astrojs/sitemap` (audited OK in 02-RESEARCH.md's Package Legitimacy Audit: official withastro package, ~2M weekly downloads, current publish). It is the only new dependency in this phase. Note in the summary that the RESEARCH-flagged legitimacy checkpoint for the remark package is moot because that package is no longer being installed.

Write `src/lib/wikilink-resolver.mjs` exporting `createWikilinkResolver({ base, technicalRoot })`. At construction it walks `technicalRoot` once with `node:fs` and builds a set of known ids in the same `<milestone>/phase-<number>-<slug>` form the `technical` collection uses, plus the how-to-read id. The returned `resolve(target, fromEntryPath)` trims the target, drops a trailing `.md`, resolves any leading relative segments against the source entry's directory using `node:path/posix`, maps the how-to-read filename to its D-32 id, looks the result up in the set, and returns a base-prefixed absolute site path with a trailing slash, or `null` on a miss. The base path arrives as an argument from `astro.config.mjs` and is never a literal in this file — SITE-02 keeps it in one config location and the smoke test forbids the literal under `src/`.

Write `src/lib/mdast-wikilinks.mjs` exporting `createWikilinkPlugin({ resolve })`, returning a Sätteri plugin definition built with `defineMdastPlugin` from the `satteri` package. Give it a `name` and a single `text` visitor. Registering on the text node type is the entire defence against the C++ attribute collision — Sätteri hands code and inline-code content to different visitor keys, so a text visitor structurally never sees them; add nothing that re-checks for that by string matching. For each text node, scan for double-square-bracket pairs with an optional pipe-separated label. If none are present, return without touching the node. Otherwise build the replacement sequence — literal text segments interleaved with link nodes whose url is the resolved href and whose only child is a text node carrying the label when present and the target otherwise — then splice it in via the visitor context's insert-before (which accepts an array) followed by removing the original node. On a `null` resolve, throw immediately with a message naming both the source file (from the visitor context's file URL) and the raw bracketed text (D-39: loud build failure, never a silent passthrough of the literal source text).

Extend `astro.config.mjs` additively, keeping `site` and `base` as the single URL source:
  - Define the base path once as a const and pass it to both `defineConfig`'s `base` and `createWikilinkResolver`.
  - `integrations: [sitemap()]`.
  - `markdown.processor: satteri({ mdastPlugins: [createWikilinkPlugin({ resolve })] })`, importing `satteri` from `@astrojs/markdown-satteri`. Verified in the installed processor source: user mdast plugins are appended to Astro's own defaults rather than replacing them, so heading ids, image collection and Shiki all survive this change — the TOC anchors in Plan 04 depend on that.
  - `markdown.shikiConfig.theme: 'github-light'`. Astro's default is a dark theme and this site is light-only, so leaving it unset silently ships a dark code block against a near-white page (D-22/D-41, RESEARCH Pitfall 5).

Extend `tests/lib.smoke.mjs` with the resolver behaviour cases above (construct the resolver against the real `technical/` tree with a fixed test base string) AND with plugin-level compile assertions. No technical route exists yet — Plan 06 builds those — so drive the plugin directly: import `markdownToHtml` from the `satteri` package, pass `{ mdastPlugins: [createWikilinkPlugin({ resolve })] }`, and assert against small in-memory Markdown samples that a labelled wikilink compiles to an anchor carrying the resolved href and the label as its text; that a fenced C++ block containing the attribute compiles with that attribute intact and produces no anchor; that inline backtick-wrapped code does the same; and that an unresolvable target throws a message naming both the file and the bracketed text. Write these assertions before the plugin exists and confirm they fail first.

Create a NEW harness file `tests/markdown.smoke.sh` for the build-output assertions available at this point: a sitemap file exists in `dist/` and its URLs carry the configured base path, and the built announcement pages carry Shiki's inline-style markup on any fenced code with no highlighting script emitted. Route-level wikilink assertions over the technical tree belong to Plan 06, which is where those routes first exist.
  </action>
  <verify>
    <automated>node tests/lib.smoke.mjs && npm run build && bash tests/markdown.smoke.sh && ls dist/sitemap*.xml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'markdown-remark' package.json` outputs `0` <!-- planner-discipline-allow: markdown-remark -->
    - `ls src/lib/ | grep -c '^remark-'` outputs `0` <!-- planner-discipline-allow: remark- -->
    - `grep -c 'unified' astro.config.mjs` outputs `0`
    - `astro.config.mjs` contains `satteri(`, `mdastPlugins`, `github-light`, and `sitemap()`
    - `grep -c 'interstellar-website' src/lib/wikilink-resolver.mjs` outputs `0`, and `! grep -rIn -e 'github\.io' -e '/interstellar-website' src/` still succeeds
    - `node tests/lib.smoke.mjs` exits 0 with the resolver cases AND the plugin compile cases included
    - A compile assertion proves a labelled wikilink becomes an anchor with the resolved href and the label as its text
    - A compile assertion proves a fenced C++ block containing the attribute compiles unchanged and produces no anchor
    - A compile assertion proves an unresolvable target throws with both the file and the bracketed text in the message
    - `npm run build` exits 0 over the whole 74-file corpus
    - A built announcement page containing a fenced code block carries Shiki inline-style markup and contains no `<script` tag
    - `ls dist/sitemap*.xml` lists at least one file, and its URLs carry the configured base path
    - `bash tests/markdown.smoke.sh` exits 0, and `npm test` (the run-all harness) exits 0
  </acceptance_criteria>
  <done>Wikilinks resolve to real internal anchors at build time, an unresolvable one fails the build by name, code fences highlight in a light theme with no client JS, and a sitemap is emitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown source → emitted `href` | The only place content text becomes a URL in this phase |
| npm registry → build toolchain | One new dependency enters the supply chain here |
| collection query → generated chrome | Draft entries must not cross into rendered lists |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `src/lib/wikilink-resolver.mjs` → `href` construction | high | mitigate | The resolver returns only a base-prefixed internal path built from an id present in its known-ids set, or `null`; the raw target text is never passed through to an `href`, so no scheme or absolute external URL can be injected. A `null` result throws at build time (D-39) rather than degrading |
| T-02-02 | Tampering | `npm install @astrojs/sitemap` | high | mitigate | Package Legitimacy Audit in 02-RESEARCH.md returned OK (official withastro/astro monorepo package, ~2.08M weekly downloads, current publish); version pinned via committed `package-lock.json` and CI installs with `npm ci`. The audit's one SUS entry is moot — that package is no longer installed under the amended D-39 |
| T-02-03 | Information Disclosure | Draft entry leaking into generated cross-links | medium | mitigate | `isVisible` in `src/lib/content-guards.ts` is the single draft-filter implementation (D-30); Plans 06 and 07 are required to use it for generated cross-link lists, not only for index pages |
| T-02-09 | Denial of Service | Build failure on malformed content | low | accept | Loud build failure is the intended behaviour (D-10/D-33/D-39), not a threat to mitigate — the deploy simply does not publish. Accepted deliberately: a silent skip would be strictly worse |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | Only `@astrojs/sitemap` is installed, audited above; no `[ASSUMED]` or `[SUS]` package is installed in this phase |
</threat_model>

<verification>
- `node tests/lib.smoke.mjs` exits 0
- `npm run build` exits 0 over all four collections
- `npm test` runs the whole committed harness set and exits 0
- No literal host or base string under `src/`
- No remark-pipeline package in `package.json`
</verification>

<success_criteria>
The four content trees are queryable collections with loud-fail filename rules; wikilinks resolve to internal anchors or fail the build by name; code fences highlight light-theme with zero client JS; `sitemap.xml` is emitted; and every derived-data helper the page templates need exists with committed assertions behind it.
</success_criteria>

<output>
Create `.planning/phases/02-content-rendering-templating/02-03-SUMMARY.md` when done. Record the exact Sätteri plugin shape used, so Plans 04–07 and any future Astro upgrade have the verified reference.
</output>
