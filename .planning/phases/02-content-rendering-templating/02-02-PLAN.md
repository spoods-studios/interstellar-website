---
phase: 02-content-rendering-templating
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - src/pages/404.astro
  - public/favicon.svg
  - tests/shell.smoke.sh
autonomous: true
requirements: [SITE-04]
must_haves:
  truths:
    - "Every page that adopts BaseLayout renders the same header wordmark + four-item nav and the same minimal footer"
    - "A visitor hitting a non-existent URL gets a styled 404 page with site chrome and a working link home"
    - "A favicon is served and referenced from every page"
    - "Every page emits a canonical <link> derived from the one config-driven site/base pair"
    - "The reading column is legible at 375px and 1440px with no horizontal scroll"
  artifacts:
    - src/styles/global.css
    - src/layouts/BaseLayout.astro
    - src/pages/404.astro
    - public/favicon.svg
  key_links:
    - "BaseLayout is the single <head> owner — Phase 3's OpenGraph tags and Discord CTA slot into it, so its props must not be devlog-specific"
    - "Canonical URL composes Astro.site with import.meta.env.BASE_URL — no literal host or base string may appear anywhere under src/"
    - "404.astro must build to dist/404.html at the artifact root for GitHub Pages to serve it"
---

<objective>
Build the site shell every page in this phase hangs off: a hand-written stylesheet, one `BaseLayout.astro` carrying header/nav/footer/`<head>`, a hand-authored favicon, and the custom 404 page.

Purpose: this is the chrome half of SITE-04 and the visual contract from 02-UI-SPEC.md. It lands before any content template so that the four content trees all inherit one reading surface rather than drifting apart (UI-SPEC "Layout factoring").
Output: `src/styles/global.css`, `src/layouts/BaseLayout.astro`, `public/favicon.svg`, `src/pages/404.astro`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-rendering-templating/02-UI-SPEC.md
@.planning/phases/02-content-rendering-templating/02-CONTEXT.md
@src/pages/index.astro
@astro.config.mjs
</context>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Exported / consumed as |
|---|---|---|
| `src/styles/global.css` | stylesheet | Imported once by `BaseLayout.astro`; owns fonts, palette, spacing scale, reading measure, the single 640px breakpoint, code-fence and TOC rules |
| `src/layouts/BaseLayout.astro` | Astro layout | Default export; props `{ title: string; description?: string }` plus a default `<slot />` |
| `public/favicon.svg` | asset | Served at `<base>/favicon.svg`, referenced from `BaseLayout`'s `<head>` |
| `src/pages/404.astro` | route | Builds to `dist/404.html` |
| `tests/shell.smoke.sh` | harness | Committed shell assertions for the shell artifacts |

<tasks>

<task type="auto">
  <name>Task 1: Hand-write global.css and BaseLayout.astro</name>
  <files>src/styles/global.css, src/layouts/BaseLayout.astro, public/favicon.svg</files>
  <read_first>
    - `.planning/phases/02-content-rendering-templating/02-UI-SPEC.md` — the binding contract: Design System, Spacing Scale, Typography, Color, and the whole "Layout &amp; Structural Contract" section. Every number below comes from it; do not substitute your own.
    - `src/pages/index.astro` — lines 8-9 and 42-47 show the existing `import.meta.env.BASE_URL` + `Astro.site` canonical computation that moves into this layout verbatim
    - `astro.config.mjs` — confirms `site` and `base` are the only URL source (SITE-02)
    - `tests/build.smoke.sh` — note the assertion that forbids literal host/base strings under `src/`; this layout must satisfy it. Do NOT edit this file in this plan — Plan 01 owns it in the same wave; this plan adds its own harness file instead
  </read_first>
  <action>
Write `src/styles/global.css` implementing the UI-SPEC exactly:
  - System font stack for prose and the monospace stack for code, both copied from UI-SPEC's Design System section (D-21, zero webfonts, zero external requests).
  - Palette: background `#fefefe`, text `#1a1a1a`, link accent `#1d5fae` (D-22). No `prefers-color-scheme` block, no CSS custom-property theming layer — dark mode is v2 (SITE-05) and an unused theming hook is scope creep.
  - Type scale: body 18px/1.6, labels 14px/1.4, H2–H3 22px/1.3 semibold, H1 30px/1.2 semibold. Exactly two weights, 400 and 600.
  - Spacing scale as CSS values on the seven UI-SPEC tokens (4/8/16/24/32/48/64). Do not add per-element margin resets inside the rendered Markdown body — D-14 forbids restyling the body, so only the global type/measure rules touch it.
  - Reading column: `max-width: 65ch`, centred with `margin-inline: auto` (D-23).
  - One breakpoint at 640px, mobile-first: below it the column is fluid with 16px side padding; at and above it the column is the fixed measure. No second breakpoint.
  - Header nav is a wrapping flex row — no hamburger, no client-side toggle, zero JS.
  - Code-fence rules: 8px vertical / 16px horizontal padding, monospace stack, `overflow-x: auto` (never wrap), no line numbers. Shiki emits its own inline colour styles, so do not set colours on `pre`/`code` beyond the fence chrome.
  - A `.toc` block plus the sticky-sidebar and `<details>` rules the TOC component will consume at each side of the 640px breakpoint (sticky offset 32px). Writing them here keeps all responsive rules in one file.
  - No `!important`, no framework, no classless base (D-23).

Write `src/layouts/BaseLayout.astro`:
  - Props `title` (required) and `description` (optional). Render `<html lang="en">`, `<meta charset>`, `<meta name="viewport" content="width=device-width, initial-scale=1">`, the `<title>`, and the canonical `<link>` computed from `Astro.site` and `Astro.url.pathname` — never a literal host or base string, so the smoke-test grep stays green (SITE-02). Reference the favicon with `import.meta.env.BASE_URL`.
  - Import `../styles/global.css` here and only here.
  - Header (D-17 as amended): wordmark text "Interstellar Engine" linking to the site root, plus four nav links in this order — Devblog (site root), Technical (`technical/`), How It's Made (`how-its-made/`), Roadmap (`roadmap/`). Every href is composed from `import.meta.env.BASE_URL`. No active-item colour change (UI-SPEC Color) and no Discord CTA placeholder — that slot is Phase 3's (D-17).
  - Footer (D-18): a copyright line reading `© 2026 Spoods Studios`. Do not build a disabled or placeholder RSS control — the RSS link is Phase 3's and dead chrome is worse than none.
  - A single default `<slot />` between header and footer inside the reading column.
  - Leave the `<head>` structured so Phase 3 can add OpenGraph/Twitter tags without restructuring: the optional `description` prop already exists for that reason.

Hand-author `public/favicon.svg` (D-24): a small square SVG with the near-black text colour on the near-white background, drawing an "IE" letterform using `<text>` with the same system font stack. No raster fallback, no generated imagery (D-G), no external font reference. Keep it under ~1KB so it stays hand-editable and swappable for an engine capture later.
  </action>
  <verify>
    <automated>npm run build && test -f public/favicon.svg && test -f src/styles/global.css && grep -q '65ch' src/styles/global.css && grep -q '#1d5fae' src/styles/global.css && grep -q '640px' src/styles/global.css && ! grep -rIn -e 'github\.io' -e '/interstellar-website' src/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'prefers-color-scheme' src/styles/global.css` outputs `0`
    - `grep -c '!important' src/styles/global.css` outputs `0`
    - `src/styles/global.css` contains the literals `65ch`, `#fefefe`, `#1a1a1a`, `#1d5fae`, `640px`, and `overflow-x` (one occurrence each is sufficient)
    - `grep -c '@media' src/styles/global.css` outputs `1` (single breakpoint)
    - `src/layouts/BaseLayout.astro` contains `rel="canonical"`, `Astro.site`, `import.meta.env.BASE_URL`, `viewport`, and the four nav labels Devblog / Technical / How It's Made / Roadmap
    - `! grep -rIn -e 'github\.io' -e '/interstellar-website' src/` succeeds
    - `grep -ci 'discord\|rss' src/layouts/BaseLayout.astro` outputs `0` (no Phase 3 placeholders)
    - `grep -c '<script' src/layouts/BaseLayout.astro` outputs `0` (zero client JS)
    - `npm run build` exits 0 and `test -f dist/favicon.svg` succeeds
  </acceptance_criteria>
  <done>One stylesheet and one layout implement the whole UI-SPEC shell, with no client JS, no hardcoded URLs, and no Phase 3 dead chrome.</done>
</task>

<task type="auto">
  <name>Task 2: Custom 404 page wired to the shell</name>
  <files>src/pages/404.astro, tests/shell.smoke.sh</files>
  <read_first>
    - `src/layouts/BaseLayout.astro` — the layout written in Task 1; read its prop names before importing it
    - `.planning/phases/02-content-rendering-templating/02-UI-SPEC.md` — the Copywriting Contract row for 404 copy, which is exact and must be reproduced character-for-character
    - `.planning/phases/02-content-rendering-templating/02-RESEARCH.md` — Pattern 7 and Pitfall 6: the 404 file must land at the artifact root, and the live behaviour under the Pages base path is only MEDIUM-confidence and gets an empirical check in Plan 08
  </read_first>
  <action>
Create `src/pages/404.astro` rendering through `BaseLayout` with title "Page not found" (D-19: same chrome as every other page). Body copy comes verbatim from the UI-SPEC Copywriting Contract: a heading reading `Page not found.`, one explanatory sentence about the page not existing or the link being out of date, and a link home labelled `← Back home` whose href is `import.meta.env.BASE_URL`. Nothing else — no search box, no suggested-links list.

Create a NEW harness file `tests/shell.smoke.sh` (do not edit `tests/build.smoke.sh` — Plan 01 owns that file in this same wave). Model it on `tests/build.smoke.sh`: `set -euo pipefail`, `cd` to the repo root, run `npm run build`, then assert `dist/404.html` and `dist/favicon.svg` both exist, that `dist/404.html` contains the 404 heading copy and the back-home link text, and that `dist/404.html` carries the same footer copyright line as `dist/index.html`. Make it executable. No test framework is being introduced — a committed shell script is the phase's harness convention (02-VALIDATION.md, Phase 1 precedent).

Do not attempt to verify live 404 behaviour here; that is a post-deploy check owned by Plan 08 (RESEARCH Pitfall 6).
  </action>
  <verify>
    <automated>npm run build && test -f dist/404.html && grep -q 'Page not found' dist/404.html && grep -q 'Back home' dist/404.html && bash tests/shell.smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/pages/404.astro` succeeds and it imports `BaseLayout`
    - `test -f dist/404.html` succeeds after a build
    - `grep -c 'Page not found' dist/404.html` is at least `1`
    - `grep -c 'Back home' dist/404.html` is at least `1`
    - `dist/404.html` contains the same header wordmark and footer copyright line as `dist/index.html` (same chrome, D-19)
    - `bash tests/shell.smoke.sh` exits 0, and `tests/build.smoke.sh` is unmodified by this plan (`git diff --stat tests/build.smoke.sh` is empty)
  </acceptance_criteria>
  <done>A styled 404 page builds to the artifact root and a dedicated smoke harness asserts it plus the favicon on every run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build config → emitted HTML | The site/base pair is the only URL authority; a hardcoded host in a template silently breaks a future domain move |
| visitor browser → static site | Read-only; no forms, no cookies, no client JS in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | Hardcoded host/base in `src/` | medium | mitigate | `tests/build.smoke.sh` already fails the build on any literal host or base string under `src/`; BaseLayout composes every href from `Astro.site` / `import.meta.env.BASE_URL` |
| T-02-08 | Information Disclosure | Third-party requests from page chrome | low | accept | Zero webfonts, zero analytics, zero external assets this phase (D-21); analytics arrives in Phase 4 under ANLT-01's cookieless constraint. Accepted because there is nothing to leak — no external request exists |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan; `@astrojs/sitemap` is audited and installed in Plan 03 |
</threat_model>

<verification>
- `npm run build` exits 0
- `bash tests/shell.smoke.sh` exits 0
- `dist/404.html` and `dist/favicon.svg` exist
- No literal host or base string anywhere under `src/`
- Zero `<script>` tags in built output for the shell
</verification>

<success_criteria>
Every later template in this phase can import one layout and one stylesheet and inherit the entire approved visual contract, a working 404, a favicon, and canonical URLs, with no client JS.
</success_criteria>

<output>
Create `.planning/phases/02-content-rendering-templating/02-02-SUMMARY.md` when done.
</output>
